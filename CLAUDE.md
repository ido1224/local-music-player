Project: Local Music Player (Android) — Phase 1
Overview
Native Android app (Kotlin) for local, offline music playback with a custom library and manual playlists. This is Phase 1 of a larger project — later phases add listening tracking, on-device AI recommendations, and a NERA-backed "Discover" feature. Do not build those yet. Phase 1 is playback + library only, but the schema and structure should be clean enough to extend later (see "Forward compatibility" below).
Goals for this phase

1. A working Android app that plays audio files stored in its own app-managed music folder (not full-device auto-scan).
2. A file import flow: user picks files via Android's Storage Access Framework, app copies/references them into its library and reads ID3/tag metadata (title, artist, album, genre, year, duration).
3. Standard playback controls: play/pause/skip/seek/shuffle/repeat, a visible queue.
4. Background playback with lock-screen and notification media controls.
5. Manual, user-created playlists (create/rename/delete, add/remove tracks, reorder).

Tech stack

* Language: Kotlin
* Min SDK: 26 (Android 8.0) — confirmed
* Package name: com.nera.musicplayer
* App name: Nera Music (display name only — changed 2026-08-29 from "NERA Music Player"; package name `com.nera.musicplayer` deliberately left as-is to avoid a reinstall)
* Playback: ExoPlayer (Media3) via `androidx.media3`
* Background/notification controls: `MediaSessionService` (Media3)
* Database: Room (SQLite) — for tracks, playlists, playlist_tracks
* File import: Storage Access Framework (`ACTION_OPEN_DOCUMENT`, persistable URI permissions) — do NOT request broad `MANAGE_EXTERNAL_STORAGE`
* Metadata extraction: Media3's built-in metadata retriever, or `androidx.media3.exoplayer.MetadataRetriever` — avoid adding a second tag library unless it proves necessary
* UI: Jetpack Compose (Material 3)
* Architecture: MVVM — ViewModel + Repository pattern, Room as single source of truth, StateFlow for UI state

Data model (Room) — design for extension
Even though Phase 1 only needs playback + playlists, structure these tables now so Phase 2/3 (listening history, feature vectors) can be added without migration pain:

* `Track`: id, uri, title, artist, album, genre, year, durationMs, dateAdded, fileSizeBytes
   * Leave room conceptually for a future `feature_vector` table (one-to-one with Track) — don't build it now, just don't name things in a way that collides with it later.
* `Playlist`: id, name, dateCreated, dateModified
* `PlaylistTrack`: playlistId, trackId, position (join table, ordered)

Do NOT add listening-history or feature-vector tables yet — that's Phase 2/3.
File organization
Use a standard modern Android project layout:

```
app/src/main/java/<package>/
  data/          — Room entities, DAOs, database class, repository
  playback/      — ExoPlayer wrapper, MediaSessionService, playback state
  ui/            — Compose screens (Library, Player, Playlists, Import)
  ui/theme/      — Compose theming

```

Explicitly out of scope for this phase

* Listening tracking / history logging
* Audio feature extraction (tempo, energy, etc.)
* Any AI/similarity/recommendation logic
* NERA/Ollama networking or sync
* Discover menu
* Home screen widget
* Android Auto
* Equalizer

If you find yourself writing code for any of the above, stop — that's a later phase.
Working style

* Confirm the min SDK, package name, and app name with me before scaffolding the project if they're not already decided.
* Build incrementally: get a minimal playable app (hardcoded/test file → playback works) before wiring up the full import flow.
* After each major piece (import flow, playback, playlists), pause and summarize what changed so I can test on-device before continuing.
* Prefer Android/Kotlin idiomatic patterns over cleverness — this codebase needs to be extended by future phases, including future AI-assisted work, so keep it readable.
* **Flag explicitly whenever a schema change would wipe existing data vs. migrate it, rather than silently resetting.** `fallbackToDestructiveMigration()` has been fine pre-release since there was never real user data to lose, but that stops being true once real listening history exists — from 2026-08-29 onward, call out before (or immediately after, if the version bump was part of a larger change already in flight) any `AppDatabase` version bump whether it destroys data or has a real migration path, rather than burying it in a changelog paragraph.

## Status

- Min SDK, package name, and app name confirmed (see Tech stack above).
- Milestone 1 (minimal playback pipeline) scaffolded AND verified on-device: ExoPlayer + MediaSessionService background playback, a single-file Storage Access Framework picker, and a bare Compose screen with play/pause and seek. Confirmed working on an emulator 2026-08-28 — picked a WAV via SAF, playback/pause/seek all functioned.
- Milestone 2 (Room + import flow + library) scaffolded AND verified on-device 2026-08-28: `Track`/`Playlist`/`PlaylistTrack` Room schema (Playlist/PlaylistTrack tables exist but have no DAO wiring into UI yet — that's the playlists milestone). Import uses `ActivityResultContracts.OpenMultipleDocuments()`, copies each picked file into `context.filesDir/music/<uuid>.<ext>` (app-managed storage per the Overview, not a persisted-URI reference to the original external file — deviates from the literal "persistable URI permissions" line in Tech stack, but matches "its own app-managed music folder"), and reads tags via the platform `android.media.MediaMetadataRetriever` (zero extra dependency, simpler than Media3's frame-based `MetadataRetriever` for plain title/artist/album/genre/year/duration — deviates from the Tech stack's suggested API for that reason). `HomeScreen` replaced the milestone-1 `PlayerScreen`: library list on top, persistent bottom playback bar, `+` import button in the top bar. The milestone-1 ad-hoc single-file picker/play path was removed since the library flow supersedes it. Confirmed: import copies+tags+persists correctly, library list renders, tapping a track plays it through the same ExoPlayer/MediaSessionService pipeline with live seek position.
- Milestone 3 (playlists) scaffolded AND verified on-device 2026-08-28: create/rename/delete playlists, add/remove tracks, reorder via Up/Down (position-swap, not drag-and-drop — kept simple deliberately). `PlaylistRepository` + `PlaylistViewModel` (single shared instance, holds `selectedPlaylist` so both playlist screens stay in sync without a nav library). Navigation between Home/Playlists/PlaylistDetail is a manual `sealed class Screen` + `when` in `NeraApp.kt` — no `navigation-compose` dependency added, since 3 screens didn't justify it. `PlaybackBar` was extracted out of `HomeScreen.kt` into its own file and is now shared by both `HomeScreen` and `PlaylistDetailScreen` — first version of `PlaylistDetailScreen` shipped without a bottom playback bar, which was a real bug (playback worked but was invisible/uncontrollable from that screen); caught during on-device testing and fixed same session. Confirmed end-to-end: create playlist, add track via dialog, reorder buttons correctly disable at list boundaries, remove track, play track from within playlist detail with visible/working controls.
- Milestone 4 (queue/shuffle/repeat) scaffolded AND verified on-device 2026-08-28: playback is now a real ExoPlayer queue (`Player.setMediaItems(items, startIndex, 0L)`), not a single `MediaItem`. Tapping a track in `HomeScreen` queues the whole visible library list starting at that index; tapping a track in `PlaylistDetailScreen` queues that playlist's tracks the same way. `PlayerViewModel` exposes `skipToNext()`/`skipToPrevious()` (thin wrappers over `Player.seekToNext()`/`seekToPrevious()` — safe to call unconditionally, they no-op when there's nothing to skip to), `toggleShuffle()` (flips `Player.shuffleModeEnabled`), and `cycleRepeatMode()` (cycles `Player.REPEAT_MODE_OFF → ALL → ONE → OFF`). `PlayerUiState` gained `shuffleEnabled`/`repeatMode`, kept in sync via `Player.Listener.onShuffleModeEnabledChanged`/`onRepeatModeChanged`. `PlaybackBar` grew a control row (Shuffle / Prev / Play-Pause / Next / Repeat) shared by both screens — no new UI files needed. Verified via `dumpsys media_session` in tight command batches (screenshots alone were unreliable here — see note below) that Next/Prev jump between queue items at position 0, not just natural end-of-track transitions; verified Shuffle and Repeat label toggles by screenshot, confirming the full Off→All→One→Off cycle. This completes every item in Phase 1's original goal list.
- Milestone 5 (remove tracks from library) scaffolded AND verified on-device 2026-08-29: long-press a track in `HomeScreen` (via `Modifier.combinedClickable`'s `onLongClick`) to open a `DropdownMenu` with "Remove from library" and "Delete file". "Remove from library" calls `TrackDao.delete(track)` directly with no confirmation — this already cascades to `playlist_tracks` via the `ForeignKey(onDelete = CASCADE)` on `PlaylistTrack` defined back in the playlists milestone (Room enables SQLite foreign-key enforcement by default, so no new DAO query was needed for the cascade); the file on disk is left untouched. "Delete file" shows an `AlertDialog` confirmation naming the track, then on confirm does the same DB delete plus `File(URI(track.uri)).delete()` (round-tripping via `java.net.URI`, the exact inverse of the `File.toURI()` used at import time, rather than `android.net.Uri` parsing which has less predictable path extraction for `file:` URIs with varying slash counts). Verified on-device: remove-from-library removes the DB row instantly while the file remains on disk (checked via `run-as ... ls`); delete-file's Cancel button is a no-op; confirming Delete removes both the DB row and the file from disk. No crashes.
- Testing note: the two test WAV files are 4-5s long, and each screenshot round-trip (adb screencap → pull → Read) burns real wall-clock seconds. Screenshotting after a Next/Prev tap can land after the short track has already finished/auto-advanced on its own, producing a false-looking "did nothing" result. For anything timing-sensitive with short test media, verify with `adb shell dumpsys media_session` (shows exact item id + position) in one tight command batch rather than separate screenshot round-trips.
- Emulator quirk (not an app bug): a fresh AVD's stylus-handwriting tutorial overlay can hijack input focus the first time a text field is tapped in a session. Fixed for this AVD via `adb shell settings put secure stylus_handwriting_enabled 0`. Unrelated to the app; won't reproduce on real devices.
- Emulator quirk: the Pixel_8 AVD's quickboot snapshot has failed to load (`virtio-snd` state corruption) and crashed the emulator process at least twice across sessions, always between conversation turns (not caused by anything the app did). Fix: `emulator -avd Pixel_8 -no-snapshot-load` for a cold boot. App data, installed APK, and pushed test files all survive since they live on the persisted userdata disk image, not the RAM snapshot — no need to reinstall/re-push after a cold boot, just relaunch.
- Gradle wrapper jar and `gradlew`/`gradlew.bat` are now committed (generated via the Gradle distribution Android Studio cached locally). No longer need Android Studio to regenerate them.
- Project was originally scaffolded under OneDrive at a path containing Hebrew characters; AGP refuses to build under non-ASCII paths on Windows. Moved to `C:\Users\Ido124\AndroidStudioProjects\local-music-player` (also gets it out of OneDrive sync, which can lock files mid-build). Use this path going forward.

## Phase 2 (started early, 2026-08-29)

Phase 1's Overview section explicitly lists listening tracking and audio feature extraction as out of scope until Phase 2/3. The user explicitly requested both below despite that, so treat this as Phase 2 having begun — the "stop, that's a later phase" guidance in the Overview no longer applies to these two areas specifically.

- Listening tracker: new `ListenEvent` Room entity (`data/ListenEvent.kt`) — id, trackId (FK to Track, `onDelete = CASCADE`), timestamp (epoch millis), completionPercent (Int 0-100). `ListenEventDao` has `insert` + a `count()` used for debug verification. `ListeningRepository` wraps the DAO and logs `Log.d("ListeningRepository", ...)` on every insert. `AppDatabase` bumped to version 2 with `fallbackToDestructiveMigration()` (fine pre-release, no real user data to preserve).
  - `PlayerViewModel` now tags every `MediaItem` with `setMediaId(track.id.toString())` (previously untagged) so the currently-playing track can be identified from `controller.currentMediaItem`. The existing 500ms position ticker checks `position/duration >= 0.8f`; on first crossing for the current media item it fires `listeningRepository.recordListen(trackId, completionPercent)` once, guarded by `loggedListenForMediaId` which resets in `onMediaItemTransition` (so a repeat-mode replay of the same track logs again — each listen counts, not each track-ever).
  - Verified on-device 2026-08-29: played `test_tone` (5s) past 80%, confirmed exactly one `Recorded listen event: trackId=1 completion=81%` logcat line (not two, on a later poll — dedup works). Pulled `nera_music.db` **and its `-wal`/`-shm` sidecar files** together (SQLite WAL mode means recent writes sit in `-wal` until checkpointed — pulling just the `.db` file showed an empty table; pulling all three and opening with Python's stdlib `sqlite3` showed the real row: `(id=1, trackId=1, timestamp=1787998491496, completionPercent=81)`, matching the log line exactly.
  - No UI surfaces this yet, as requested — logcat/DB is the only way to observe it for now.

- TarsosDSP spike (isolated, androidTest-only — not a dependency of the shipped app): **integration succeeded, tempo estimation was inconclusive by design of the test input.**
  - Dependency: TarsosDSP is not on Maven Central or JitPack — it's published at `https://mvn.0110.be/releases` as `be.tarsos.dsp:core:2.5` (added to `settings.gradle.kts`'s `dependencyResolutionManagement.repositories`). Only `core` was added, not `jvm` (the `jvm` module pulls in javax.sound-based desktop I/O that doesn't exist on Android — unnecessary since `core` alone has everything needed: `AudioDispatcher`, `UniversalAudioInputStream`, `TarsosDSPAudioFormat`, `PercussionOnsetDetector`).
  - Added `androidx.test.ext:junit`, `androidx.test:runner`, `junit:junit`, and `testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"` — this project had zero test infrastructure before this spike.
  - `app/src/androidTest/java/com/nera/musicplayer/TarsosDspSpikeTest.kt`: loads `test_tone_2.wav` bundled as an androidTest asset (`app/src/androidTest/assets/`, copied from the same file used in earlier milestones), parses the RIFF/WAVE header manually (scans chunks rather than assuming a fixed 44-byte offset — more robust than hardcoding), wraps the raw PCM bytes in `UniversalAudioInputStream` + `TarsosDSPAudioFormat` (avoids `javax.sound.sampled` entirely, which isn't available on Android — this is TarsosDSP's documented Android-compatible I/O path), feeds it through `AudioDispatcher` + `PercussionOnsetDetector`, and logs everything to logcat under tag `TarsosDspSpike`. Deliberately has no assertion on onset count or BPM — a passing test here means the library built/linked/ran without throwing, which is the actual thing being validated.
  - `./gradlew assembleDebugAndroidTest` succeeded first try (dependency resolved, all constructor signatures matched real TarsosDSP source verified via GitHub before writing the code, no duplicate-class or desugaring issues).
  - `./gradlew connectedDebugAndroidTest` ran clean on the emulator. Logcat: `Parsed WAV: sampleRate=44100 channels=1 bitsPerSample=16 dataSize=352800 bytes` (352800 = 44100 Hz × 4s × 2 bytes/sample, checks out exactly against the known test file), then `Onsets detected: 0`.
  - **Why zero, and why that's not a failure**: `test_tone_2.wav` is a sustained pure sine tone (generated for earlier playback-testing milestones, not for this purpose) with no percussive attacks. `PercussionOnsetDetector` triggers on many FFT bins jumping in energy simultaneously (a broadband transient); a pure tone's energy sits in one bin, so it correctly never fires. Zero onsets on non-rhythmic audio is TarsosDSP working as designed, not garbage output — the user's own instructions said to use "one of the existing test audio files," and all of them are pure tones, so this result was foreseeable going in.
  - **Net finding from the sine tone alone**: the library builds, links, and runs cleanly. No conclusion possible about detection *accuracy* from a non-rhythmic signal.

- Follow-up, same spike (2026-08-29): added `click_track_120bpm.wav` — a synthetic asset generated specifically for this test (`app/src/androidTest/assets/`, not reused from playback milestones), 16 short broadband noise-burst "clicks" (~15ms, fast attack/exponential decay) exactly every 500ms = 120 BPM ground truth, 8s total. `TarsosDspSpikeTest` refactored: `runOnsetDetection(assetName)` extracted as a shared helper, two `@Test` methods (sine tone, click track), and `logTempoEstimate()` compares against a known-good BPM when one exists, logging a `MATCH`/`MISMATCH` verdict (±5% tolerance) — deliberately not a hard JUnit assertion, so a tuning miss doesn't read as "library integration broke."
  - **First attempt returned 0 onsets on the click track too** — surprising, since real percussive content should trigger the detector. Diagnosed by re-reading `PercussionOnsetDetector.process()` (already fetched from GitHub for the constructor signature): it guards `if (priorMagnitudes[i] > 0.f)` before counting a bin as "over threshold" — a bin whose *previous* frame had exactly zero energy is skipped entirely. My first click track had true digital silence (sample value `0`) between clicks, so most frames immediately preceding a click had `currentMagnitudes` (and thus the next frame's `priorMagnitudes`) at exactly `0.0f`, silently defeating the diff-based detector. This is a real property of the algorithm — it assumes a nonzero noise floor, which real-world recordings always have and pure synthetic silence doesn't.
  - **Fix**: regenerated the click track with a small dither noise floor (±40 out of 32767, inaudible) between clicks, so no bin is ever exactly zero-energy. This is a correction to an unrealistic property of the *test asset*, not tuning the library to force an answer.
  - **Result after the fix**: 4 onsets detected (not 16) at `[4.04s, 5.04s, 6.04s, 7.04s]` — evenly spaced exactly 1.0s apart, i.e. **every other click**, starting only at click #9 of 16. Computed tempo: **60.1 BPM vs. 120 BPM ground truth (49.9% error, MISMATCH)**. This is a textbook **octave error** (locking onto half the true tempo) — a known failure mode in beat-tracking algorithms in general, not random garbage: the detector found something real and periodic, just at the wrong rate, and undercounted rather than overcounted or produced noise.
  - **Working theory** (not verified further, per instructions to stop and report): `PercussionOnsetDetector`'s defaults (`DEFAULT_SENSITIVITY = 20`, `DEFAULT_THRESHOLD = 8`) require over 80% of all 512 frequency bins to jump >8dB within a single frame to register a peak — a strict bar. With `bufferOverlap = 0`, a 15ms click can land across a 1024-sample (~23ms) frame boundary and split its energy across two frames, sometimes failing to clear that bar; per-click random noise variance plausibly pushes alternating clicks above/below the threshold.
  - **Net finding, updated**: TarsosDSP's `core` artifact is confirmed to build, link, and run cleanly end-to-end on real PCM audio, and its onset detector does find real periodic percussive events (not noise) — but **the default `PercussionOnsetDetector` configuration is not usable as-is** for reliable tempo detection; it undercounted by exactly half on a clean, unambiguous 120 BPM click track.

- Second follow-up, same spike (2026-08-29) — **superseded, see below**: added `edm_clip.wav`, 8 real seconds (45s-53s) of an actual EDM track extracted from `~/Downloads/W&W - OIIA OIIA (Spinning Cat).mp3` (already on this machine, not downloaded for this purpose). This was **copyrighted commercial audio and has since been deleted from the repo** (2026-08-29, same day) at explicit user request and replaced with `ncs_clip.wav` (royalty-free NCS release, see further below) — do not try to recreate `edm_clip.wav` or pull that MP3 back in. The findings below are kept for the record since they're still accurate/instructive, but the asset itself no longer exists.
  - `PercussionOnsetDetector` rerun with `bufferOverlap` at 50% (512 of a 1024-sample buffer, up from 0) against `edm_clip.wav`: **0 onsets** — a complete miss, worse than the click-track result, on real mixed/mastered music. Overlap alone did not fix it.
  - Per instructions, tried `ComplexOnsetDetector` (spectral-flux based, `fftSize` matched to the 1024-sample buffer, default `peakThreshold=0.3`) as the alternative, same clip, same 50% overlap: **66 onsets detected in 8 seconds**, inter-onset intervals mostly clustering around 0.093-0.105s. Naive median-interval-to-BPM math implies **~574 BPM**, which is not a plausible kick-drum tempo for any real track.
  - **Reading of this result**: `ComplexOnsetDetector` is clearly responding to real transient energy in the mix (unlike `PercussionOnsetDetector`'s 0 here) — but at ~8 onsets/second it's picking up most percussive/transient content in the full mix (hi-hats, snare, synth stabs, general spectral flux), not selectively isolating the kick/beat pulse. Raw onset count ≠ tempo: a real tempo estimate from these onsets would need a proper beat-tracking layer (e.g. an onset-strength autocorrelation or a histogram over many pairwise inter-onset-interval candidates to find the dominant periodicity, filtering to isolate low-frequency/kick energy before onset detection, or a `peakThreshold` well above the 0.3 default to reject smaller transients) — not a naive "sort consecutive gaps, take the median" computation, which blends multiple simultaneous rhythmic layers (kick + hi-hat + other percussion) into one meaningless number.
  - **Net finding, this round**: neither onset detector, used with a naive median-inter-onset-interval tempo calculation, produces a usable BPM on real mixed music: `PercussionOnsetDetector` under-detects, `ComplexOnsetDetector` over-detects. Confirmed the fix needed was TarsosDSP's actual beat-tracking layer, not more tuning — see below.

- **Third follow-up, same spike (2026-08-29) — validated. This is the working answer.** Deleted `edm_clip.wav` (copyrighted W&W commercial track) at explicit user request. Replaced it with `ncs_clip.wav`: 8 real seconds (60s-68s) of **"Elektronomia - Sky High" [NCS Release]** — royalty-free, downloaded directly from `https://ncs.io/track/download/6d744632-0472-463c-8a13-da071971fbdf` (the real download link behind `ncs.io/skyhigh`, verified via HTTP headers before saving: `Content-Type: audio/mpeg`, `Content-Disposition: attachment; filename="Elektronomia - Sky High [NCS Release].mp3"`) with the user's explicit go-ahead. Safe to keep in the repo long-term, unlike the file it replaces. Documented tempo cross-checked across Tunebat/SongBPM before picking this track: **~128 BPM** (single most common figure; a couple of sources say 129-130). Verified the extracted clip isn't silence: peak=32768 (clipping/mastered-loud), RMS=13264/32767.
  - Used TarsosDSP's actual intended beat-tracking API this time, not raw onset detection: `BeatRootOnsetEventHandler` implements `OnsetHandler` itself, so it's handed to `ComplexOnsetDetector` as a passive collector during one `dispatcher.run()` pass over the audio (bufferSize=1024, 50% overlap); afterward, `beatRootHandler.trackBeats(handler)` runs BeatRoot's actual induction + multi-agent beat-tracking algorithm (tempo induction from over recurring inter-onset intervals, then agents scoring different tempo/phase hypotheses against the onset list) as a post-processing step, reporting real beat times through a second `OnsetHandler` callback — a fundamentally different, much smarter computation than "sort onsets, take the median gap."
  - **Result: 17 beats detected across the 8s clip** at `[0.33, 0.8, 1.29, 1.74, 2.2, 2.68, 3.15, 3.62, 4.08, 4.56, 5.02, 5.5, 5.95, 6.43, 6.895, 7.36, 7.83]` — visibly, consistently spaced ~0.45-0.49s apart throughout (128 BPM's exact quarter-note interval is 60/128 = 0.469s). Computed tempo: **127.66 BPM vs. 128 BPM ground truth — 0.3% error, MATCH.**
  - **Second-genre validation, same day**: added `ncs_clip_trap.wav` — 8 real seconds (45s-53s) of **"JPB - High" [NCS Release]**, trap (not four-on-the-floor — syncopated hi-hats, half-time feel), downloaded from `https://ncs.io/track/download/d3abfde5-f0e5-4574-b0ba-7e0875fe89f3` with explicit go-ahead, same header-verification approach as the first track. Documented tempo has the classic trap half/double-time ambiguity: 75 BPM felt / 150 BPM measured (both are "correct" — a real property of the genre). Ran the identical `ComplexOnsetDetector` → `BeatRootOnsetEventHandler.trackBeats()` pipeline: **20 beats detected, spaced exactly 0.4s apart, computed tempo 150.00 BPM — 0.0% error** against the measured-tempo reading.
  - **Net finding, final for this spike, all rounds considered — TarsosDSP beat tracking is validated for real use.** TarsosDSP's `core` artifact builds, links, and runs cleanly in this Kotlin/Gradle/Android project. Raw onset detection alone (`PercussionOnsetDetector`, `ComplexOnsetDetector`) is not usable for tempo estimation on real music without a beat-tracking layer on top — under- or over-counts depending on the detector. **`BeatRootOnsetEventHandler` wrapping `ComplexOnsetDetector` is the validated, working combination**, confirmed across two genres with very different beat structures: 0.3% error on progressive house (four-on-the-floor), 0.0% error on trap (syncopated/half-time). Any real feature-extraction pipeline for tempo/BPM should use this combination (`ComplexOnsetDetector` → `BeatRootOnsetEventHandler.trackBeats()`), not raw onset timestamps with manual interval math. Caveats carried into pipeline design below: only validated on 8-second clips (not full tracks); BeatRoot's own doc notes it "does not work in real-time" (two-pass: collect all onsets, then track beats) — fine for offline per-track analysis at import time, not a live/streaming use case; trap-style genres can report either the felt or measured tempo (half/double) — both are musically valid, not a bug to fix.

## Pipeline design: tempo analysis at import time (2026-08-29, done and verified)

Per explicit user direction: run the validated `ComplexOnsetDetector` → `BeatRootOnsetEventHandler` pipeline once per track at import time (not per-play), cache the result in Room alongside `Track`.

- **TarsosDSP promoted from androidTest-only to a real app dependency** (`implementation(libs.tarsosdsp.core)` in `app/build.gradle.kts`, alongside the existing `androidTestImplementation` used by the spike). The spike test (`TarsosDspSpikeTest`) and its assets are left in place as regression coverage / documentation of the validation trail — not deleted now that the pipeline is real.
- **`data/TrackAudioFeatures.kt`**: one-to-one with `Track` (`trackId` is both PK and FK, `onDelete = CASCADE` — matches the pattern already used for `ListenEvent`/`PlaylistTrack`). Fields: `bpm: Float?`, `analyzedAt: Long`. Deliberately minimal — just what's needed now, not a speculative full feature-vector row (CLAUDE.md's original Phase 1 note about leaving room for a future `feature_vector` table still applies to anything beyond bpm). `TrackAudioFeaturesDao` has `upsert` (REPLACE on conflict, since re-analysis should overwrite) and `getForTrack`. `AppDatabase` bumped to version 3 (still `fallbackToDestructiveMigration()` — no real user data to preserve yet).
- **`analysis/AudioDecoder.kt`** (new package): decodes an arbitrary audio file — whatever format Android's platform `MediaCodec` supports (MP3, AAC, FLAC, WAV, OGG, ...), not just WAV like the spike's test assets — to mono 16-bit PCM via `MediaExtractor` + `MediaCodec`. Real imported files can be any format Media3 can play, unlike the spike's synthetic/extracted WAV fixtures. Downmixes multi-channel audio to mono by averaging channels (the validated spike pipeline was mono-only; stereo interleaved samples fed directly to TarsosDSP would be misread as alternating-channel noise). Seeks past the first 20s of the track before decoding (skipped if the track is too short for that to make sense) to avoid analyzing a likely quiet/silent intro, and caps decoding at 20s of source audio regardless of track length, so analysis time stays bounded independent of track duration.
- **`analysis/TempoAnalyzer.kt`**: the exact validated pipeline from `TarsosDspSpikeTest`, extracted so both the test and the real repository call the same code — `ComplexOnsetDetector(bufferSize=1024)` feeding `BeatRootOnsetEventHandler`, `dispatcher.run()`, then `trackBeats()`, then median inter-beat-interval → BPM. Returns `null` if fewer than 2 beats were found (e.g. silence, or analysis genuinely failed to find anything) rather than a bogus number.
- **`data/MusicRepository.kt`**: `importTracks()` now calls `analyzeTempo(trackId, destFile)` right after inserting each `Track` (using the Room-generated `trackId`, not re-querying). Wrapped in try/catch — a failed analysis is logged (`Log.w`) and skipped, not allowed to fail the whole import; a track with no cached tempo is a normal, expected state, not an error state elsewhere in the app.
- **Verified end-to-end on-device 2026-08-29**, through the real app UI (not a test harness): pushed `sky_high_ncs.mp3` (the same royalty-free NCS track from the spike) to the emulator, imported it through the actual SAF picker flow. Logcat: `Analyzed track 1: bpm=127.65958` — matches the spike's 127.66 BPM finding on the same track almost exactly, despite going through a completely different code path (real `MediaCodec` MP3 decode, not a pre-extracted WAV asset) and a different 20-second window of the track (seek-past-20s-then-decode, vs. the spike's manually chosen 60s-68s clip) — good evidence the result is robust regardless of which window gets analyzed, not a fluke of one specific clip. Confirmed persistence by pulling `nera_music.db` + `-wal`/`-shm` and querying directly: `track_audio_features: [(trackId=1, bpm=127.65957641601562, analyzedAt=1788006315330)]`. No crashes; confirmed no regression by playing the imported track normally afterward.
- Not yet done, out of scope for this round: no UI surfaces the cached BPM anywhere (matches "no UI needed yet" pattern from the listening-tracker milestone — this is backend/data-layer only for now).

## BPM in the UI + real-world batch import test (2026-08-29, done and verified)

- **`data/TrackWithBpm.kt`**: `@Embedded val track: Track` + `val bpm: Float?` — a flat join POJO, not a Room `@Relation`, populated by a raw `LEFT JOIN` query. Chosen over `@Relation` because it's simpler for a true 1:1 (no list/set wrapper to unwrap) and avoids reflection-based relation-fetching overhead for what's just two extra columns.
- **`data/LibrarySortOrder.kt`**: `DATE_ADDED` / `BPM` enum.
- **`TrackDao`**: two new queries, `observeAllWithBpmByDateAdded()` and `observeAllWithBpmByTempo()` (`ORDER BY track_audio_features.bpm IS NULL, track_audio_features.bpm ASC` — the `IS NULL` first term pushes un-analyzed tracks to the end of a tempo sort instead of clumping them at the top ahead of real values). `MusicRepository.observeTracksWithBpm(sortOrder)` picks the right one. The original `tracks: Flow<List<Track>>` / `trackDao.observeAll()` were left untouched — `PlaylistDetailScreen`'s add-tracks dialog still uses the plain version, since it doesn't need bpm and changing it would've been unnecessary churn.
- **`LibraryViewModel`**: added `sortOrder: StateFlow<LibrarySortOrder>` + `tracksWithBpm: StateFlow<List<TrackWithBpm>>` (via `flatMapLatest` over sort order) + `setSortOrder()`. `HomeScreen` now renders from `tracksWithBpm` instead of the plain `tracks` flow; `playQueue()` calls map back to `List<Track>` via `tracks.map { it.track }` since `PlayerViewModel` only needs plain tracks.
- **UI**: a `TextButton` in the top bar toggles "Sort: Date" / "Sort: BPM" (same toggle-label idiom already used for Shuffle/Repeat in `PlaybackBar` — kept consistent rather than introducing a dropdown for a binary choice). `TrackRow` gained a `bpm: Float?` param rendered via `ListItem`'s `trailingContent` as `"BPM: <rounded int>"`; the content lambda itself is `bpm?.let { value -> { Text(...) } }` — when `bpm` is null, `trailingContent` is null and nothing renders, not a placeholder or "BPM: --" or "0". **Verified visually**: imported `test_tone.wav` (known from the TarsosDSP spike to produce 0 onsets on a pure sine tone) and confirmed its row shows no badge at all, sorted to the very end of the BPM-sorted list — exactly the specified behavior, not just believed-correct-by-code-review.
- **Real-world batch import test**: user had no local NCS/Bandcamp collection to pull from (checked Downloads and OneDrive — only 4 unrelated/copyrighted MP3s existed locally), so downloaded 3 more real NCS tracks with explicit per-track confirmation, same protocol as the TarsosDSP spike (verified via HTTP headers before saving each one). One candidate (Tobu - Hope) turned out to have been privated by NCS in 2023 (artist's contract expired) — swapped for Jim Yosef - Firefly instead rather than sourcing the unavailable track from an unofficial mirror. Final batch across 4 genres: Cartoon - On & On (drumstep, 174/87 BPM ambiguity), Jim Yosef - Firefly (progressive house, 130 BPM), Culture Code - Make Me Move (dance pop, 95 BPM), plus the already-downloaded Sky High and JPB - High.
  - Imported all 5 in **one multi-select SAF batch** (the picker's actual confirm control turned out to be a button labeled "Select" in the top bar, not an icon — found via `adb shell uiautomator dump` after screenshots alone didn't make it obvious).
  - **Result: 5/5 tracks got a cached BPM, zero analysis failures.** Cross-checked against documented tempo: Firefly 130.43 vs 130 (0.3% error), JPB - High 150.0 vs 150 (exact), On & On 173.63 vs 174 (0.2% error), Sky High 127.66 vs 128 (0.3% error, consistent with every earlier measurement of this same track). Make Me Move came back 190.48 vs its documented 95 BPM — exactly double (190.48 ≈ 2 × 95.24), the same half/double-time octave ambiguity already characterized and accepted during the TarsosDSP validation spike, not a new failure mode.
  - **UI stayed responsive throughout**: a screenshot taken mid-batch (before all 5 finished analyzing) showed the list already partially populated with a correct `BPM: 128` badge on the first-completed track while later ones were still processing — the import runs inside `withContext(Dispatchers.IO)` in `MusicRepository`, so there's no plausible path for it to block the main thread, and this was confirmed empirically, not just architecturally.
  - **Confirmed via direct DB query** (pulling `.db`+`-wal`+`-shm` together, same method as prior milestones): all 6 rows (5 new + 1 pre-existing from an earlier session that survived across an emulator cold-boot) had non-null, plausible bpm values.
  - One accidental duplicate: the file-picker selection included `sky_high_ncs.mp3` a second time (already in the library from a previous session), so it got re-imported as an independent second row with its own independently-recomputed bpm (127.66, matching the first copy). Handled gracefully — no crash, no dedup logic exists or was requested; each import just creates its own Track+TrackAudioFeatures pair regardless of whether the same underlying file was imported before. Worth a dedup feature someday but out of scope here.
  - A tap-target mis-click during testing (hit `sky_high_ncs.mp3` instead of `test_tone.wav` due to misjudging scroll position) produced a 3rd `sky_high_ncs` row — not an app bug, a testing-tool-use error; left in place rather than cleaned up since it didn't affect the results being verified and DB state is disposable emulator data.
  - No crashes, no ANRs, across the entire batch-import + sort-toggle + null-bpm-display verification pass.

## Energy extraction (2026-08-29, done — validation mostly confirms it, one caveat before UI)

- **`TrackAudioFeatures`** gained `energy: Float?` (normalized RMS in [0, 1]). `AppDatabase` bumped to version 4 (still `fallbackToDestructiveMigration()`).
- **`analysis/EnergyAnalyzer.kt`** (new): plain RMS over the decoded PCM16 samples, linearly normalized against full-scale 16-bit amplitude (`rms / 32768.0`, clamped to [0,1]). Deliberately no loudness curve (dBFS/LUFS/A-weighting) — per instructions this is much simpler DSP than beat detection, and a relative intensity measure is all that's needed, not a perceptual loudness model.
- **`MusicRepository.analyzeAudio()`** (renamed from `analyzeTempo`): decodes the file **once** and derives both `bpm` and `energy` from the same `DecodedAudio`, per instructions — no second decode pass. Both are written to the same `TrackAudioFeatures` upsert.
- **Validation**: re-imported the same 5 real NCS tracks from the batch-import milestone (fresh rows post-migration, since the v3→v4 destructive migration wiped the old ones — expected, no real user data to preserve). Results (`bpm`, `energy`):
  - Jim Yosef - Firefly (progressive house): 130.43 BPM, **energy 0.173**
  - JPB - High (trap): 150.0 BPM, **energy 0.316**
  - Culture Code - Make Me Move (dance pop): 190.48 BPM, **energy 0.213**
  - Cartoon - On & On (drumstep): 173.63 BPM, **energy 0.238**
  - Sky High (progressive house): 127.66 BPM, **energy 0.340**
  - Tempo values match the batch-import milestone's findings almost exactly (same pipeline, same seek-past-20s-then-decode-20s window) — expected, confirms the refactor to share one decode pass across both analyzers didn't change tempo behavior.
  - **Sanity check, as instructed**: does trap/drumstep score higher than dance pop? **Yes for trap** (JPB - High 0.316 vs Make Me Move 0.213, a clear ~48% relative difference) — **yes but marginally for drumstep** (On & On 0.238 vs 0.213, only ~12% higher). The core requested comparison holds in both cases, more convincingly for trap than drumstep.
  - **Caveat worth flagging before wiring into UI**: Firefly (progressive house, a genre generally as energetic as Sky High) scored the *lowest* of all five (0.173) — lower than the "calmer" dance pop track. Since tempo detection on this same window matched documented BPM to within 0.3% (so the window is landing on real rhythmic content, not silence), the likely explanation is that RMS amplitude and "how energetic a track feels" aren't the same thing at a single 20-second sample point — the fixed skip-20s/take-20s window can land on a quieter breakdown or melodic passage in one track and a dense drop in another, even within the same genre. This is an inherent limitation of measuring amplitude at one fixed window position per track rather than a genre-accuracy or math problem — the RMS calculation itself isn't in question, only whether one sampled window is representative of "how energetic this track is" as a whole. Not fixed in this round per instructions (single validation pass); worth deciding whether this matters before/while wiring into UI — options if it does: sample multiple windows and average, analyze the loudest window found by scanning, or accept it as a known limitation of a fast, single-window heuristic.
  - No crashes, no ANRs.

## Energy extraction fix: multi-window sampling (2026-08-29, done and verified)

Per explicit user direction, replaced the single fixed 20-second window with three short windows sampled at ~25%/50%/75% of track duration (3s each), averaging their RMS — rather than accepting the single-window limitation found in the previous round.

- **`AudioDecoder`** refactored: the old `decodeMonoPcm16()` (single continuous window, skip-intro heuristic) is preserved as-is for tempo — BeatRoot's induction needs one uninterrupted onset timeline; three disjoint snippets would fragment that and likely break tempo detection, so tempo's decode strategy was deliberately left untouched. Added `decodeWindowAt(filePath, startMs, maxDurationMs)` (decode a short window at an explicit caller-chosen position, no heuristic) and `getDurationMs(filePath)` (cheap duration lookup via `MediaExtractor`'s track format, no decode) as the new primitives energy needed. The actual seek+decode+downmix loop was extracted into a shared private `decodeWindow()` so both public entry points reuse the same core logic.
- **`EnergyAnalyzer`** rewritten as self-contained: `computeNormalizedRms(filePath: String)` now looks up duration, computes three start positions (25%/50%/75%), decodes a 3-second window at each via `decodeWindowAt`, computes RMS per window, and averages the three RMS values before normalizing. Falls back to a single window from the start if the track is too short for three distinct non-overlapping windows (guards against tiny test files). No longer takes a shared `DecodedAudio` — tempo and energy now decode independently, since they need fundamentally different windowing (this was the point: they can no longer share one decode pass, unlike the original design).
- **`MusicRepository.analyzeAudio()`** updated to call `AudioDecoder.decodeMonoPcm16()` for tempo and `EnergyAnalyzer.computeNormalizedRms(file.absolutePath)` for energy separately (the latter manages its own decoding internally now).
- **Re-ran against the same 5 real tracks** (fresh rows, ids 6-10 — no schema change this round so old rows from the single-window version are still in the DB for direct before/after comparison):

  | Track | Genre | Old energy (1 window) | New energy (3-window avg) |
  |---|---|---|---|
  | Sky High | progressive house | 0.340 | 0.350 |
  | JPB - High | trap | 0.316 | 0.295 |
  | On & On | drumstep | 0.238 | 0.270 |
  | Firefly | progressive house | 0.173 | **0.264** |
  | Make Me Move | dance pop | 0.213 | 0.215 |

  Tempo values were identical to the previous round (130.43/150.0/190.48/173.63/127.66) — confirms decoupling tempo's decode from energy's didn't disturb tempo behavior at all, as expected since tempo's code path wasn't touched.
- **Firefly's ranking now makes sense.** It moved from *lowest of all five* (below the "calm" dance pop track — the exact anomaly flagged last round) to solidly mid-pack, just under drumstep and clearly above dance pop (0.264 vs 0.215). Averaging three spread-out windows instead of trusting one fixed point evidently smoothed out whatever quiet passage the single 20-40s window had originally landed on for this particular track.
- **Final ranking**: Sky High > JPB-High (trap) > On & On (drumstep) > Firefly > Make Me Move (dance pop) — dance pop is now unambiguously the lowest-energy track of the five, and both trap and drumstep sit clearly above it with wider margins than before (trap ~37% higher, drumstep ~26% higher, both up from the previous round's ~48%/~12% - drumstep's margin over dance pop nearly doubled, specifically fixing the "only marginally higher" weak spot from the last validation).
- No crashes, no ANRs. Not yet wired into UI, per instructions — report back first.

## Energy in the UI (2026-08-29, done and verified)

Wired the same way BPM was — extended the existing pattern rather than building a parallel one.

- **`data/TrackWithBpm.kt` renamed to `data/TrackWithFeatures.kt`**, gained `energy: Float?` alongside `bpm: Float?`. The old name would have been misleading once it carried both fields.
- **`LibrarySortOrder`** gained a third value, `ENERGY`.
- **`TrackDao`**: the two existing queries (`observeAllWithBpmByDateAdded`/`observeAllWithBpmByTempo`, renamed to `...WithFeaturesBy...`) now also select `track_audio_features.energy as energy`; added `observeAllWithFeaturesByEnergy()` (`ORDER BY energy IS NULL, energy ASC` — same null-pushed-to-end convention as the BPM sort, kept consistent rather than inventing a different rule for energy). `MusicRepository.observeTracksWithBpm` renamed to `observeTracksWithFeatures`, adds the `ENERGY` branch.
- **`LibraryViewModel.tracksWithBpm`** renamed to `tracksWithFeatures`.
- **UI**: the sort `TextButton` now cycles three ways (`DATE_ADDED → BPM → ENERGY → DATE_ADDED`) instead of toggling two. `TrackRow` gained an `energy: Float?` param; `ListItem`'s `trailingContent` now renders a small `Column` stacking `"BPM: <n>"` above `"Energy: <0.00>"` (2 decimal places), each line independently omitted when its value is null — chose a second stacked text label over a bar/dot indicator to stay visually consistent with the existing BPM badge rather than introducing a second visual language for what's conceptually the same kind of thing (a cached per-track number).
- **Verified on-device**, all 10 tracks in the library (5 from the single-window energy round + 5 from the 3-window fix, both sets still present since no schema change happened in between):
  - Both badges render stacked and correctly formatted for every track that has them (e.g. `sky_high_ncs` → `BPM: 128` / `Energy: 0.35`, matching the raw `0.3499208...` value rounded to 2dp).
  - **Sort: BPM** verified ascending (128, 128, 130, 130, 150, 150, 174, 174, 190, 190).
  - **Sort: Energy** verified ascending (0.17 → 0.35 across all ten).
  - **Independent null handling confirmed with a real edge case**, not just code review: imported `test_tone.wav` (0 onsets → `bpm = null`, but real nonzero amplitude → `energy` a genuine non-null value, 0.26). Its row shows **only** an `Energy: 0.26` label, no BPM line at all — confirming the two badge lines are independently omitted, not an all-or-nothing pair. In the BPM-sorted view it correctly sorts to the very end (null bpm); in the energy-sorted view it sorts by its real 0.26 value in the middle of the list, not pushed anywhere special (since energy isn't null for it).
  - 3-way sort cycle wraparound confirmed (Energy → Date → BPM).
  - No crashes, no ANRs.
- **Independently re-verified 2026-08-29** in a follow-up session (fresh `assembleDebug` + reinstall on the same emulator, `uiautomator dump` + a screenshot rather than trusting this log): all findings above reproduced exactly. Date sort showed ids 11→1 descending; BPM sort showed 128/128/130/130/150/150/174/174/190/190 ascending with the null-bpm `test_tone` pushed last; Energy sort showed 0.17→0.35 ascending with `test_tone`'s real 0.26 seated mid-list (not pushed to an end); every rendered badge matched the DB (`track_audio_features`, pulled via `adb exec-out run-as ... cat` — plain `adb shell ... > file` corrupted the binary via CRLF translation on Windows, exec-out avoided it) to 2dp; sort-button wraparound confirmed; no FATAL/AndroidRuntime crashes in logcat for `com.nera.musicplayer` across the whole pass.

## Similarity engine v1: BPM + energy (2026-08-29, done and verified)

- **`similarity/SimilarityEngine.kt`** (new package): pure Kotlin, no Android/Room dependency — operates on `TrackWithFeatures`. `normalizedBpmDistance` takes the minimum of the direct, doubled, and halved comparisons (per the known octave-ambiguity finding from the TarsosDSP validation) and divides by the larger of the two tempos, giving a relative 0-1 distance rather than a raw BPM gap. `normalizedEnergyDistance` is just `|e1-e2|`, already 0-1. `distance()` combines them 50/50 (`BPM_WEIGHT = ENERGY_WEIGHT = 0.5f`, per instructions to start roughly equal) and returns `null` if either track is missing bpm or energy. `rankSimilar(seed, candidates, limit)` filters out the seed itself and any candidate missing bpm/energy (excluded, not scored as maximally different, per instructions), sorts ascending by distance, and caps at `limit`; returns empty if the seed itself lacks bpm/energy since nothing can be computed against it.
- **`LibraryViewModel.findSimilarTracks(seed, limit = 20)`** wraps `SimilarityEngine.rankSimilar` against the current `tracksWithFeatures.value`.
- **UI — no pre-existing "more like this" button was found anywhere in the codebase** despite the request's premise; the only per-track action surface from Phase 1 is `HomeScreen`'s `TrackRow` long-press `DropdownMenu` (Remove from library / Delete file). Added "More like this" as a new item there. `TrackRow` gained `onMoreLikeThis: (() -> Unit)?` — `HomeScreen` passes `null` (item hidden from the menu entirely, not shown-but-disabled) when the tapped track's bpm or energy is null, so there's no dead-end tap on unanalyzed tracks. When present, the callback computes the ranked list and calls the existing `PlayerViewModel.playQueue(listOf(seed.track) + similar.map { it.track }, 0)` — reuses the same queue-replacement primitive the library/playlist screens already use, no new playback path.
- **Verified on-device**: built, installed, long-pressed the `JPB - High [NCS Release]` row (trap, one of two duplicate copies in the library from earlier batch-import testing) and tapped "More like this." Confirmed via `dumpsys media_session` (`metadata: size=... description=...`) that playback started immediately at the tapped track, then stepped through the full resulting 10-item queue via the Next button, reading the description at each step. The exact title sequence — JPB-High (duplicate) → Sky High → On & On → Firefly → Sky High → On & On → Firefly → Make Me Move → Make Me Move — **matched a hand-computed ranking from the raw DB bpm/energy values position-for-position**, including the near-identical duplicate `JPB-High` row correctly coming out as the single most similar track (distance ≈0.01, far closer than anything else) — a good sanity check that the formula and its wiring are doing exactly what the code says, not coincidentally close.
- **Genre sanity check requested in the task**: does "more like this" on the trap track surface drumstep before dance-pop? **Yes, clearly** — On & On (drumstep) lands at queue positions 3 and 6; Make Me Move (dance pop) lands at positions 8 and 9, dead last. Re-run restricted to just the 5 canonical library tracks (one row each, no duplicates) gives the same shape: On & On ≈ Firefly (near-tie, ≈0.080 vs ≈0.081 distance) > Sky High (≈0.102) > Make Me Move (≈0.146, clearly the outlier). The Firefly/On & On near-tie is a legitimate output of an equally-weighted 2-feature model on only 5 tracks, not a bug — Firefly's bpm is closer to the trap seed's, On & On's energy is closer; they land almost exactly on top of each other with this weighting.
- No crashes across the whole test.

## AI-generated playlists v1: k-means on BPM + energy (2026-08-29, backend done and verified — UI not touched yet, per instructions)

- **`similarity/ClusterEngine.kt`** (new, pure Kotlin, same testing approach as `SimilarityEngine`): Lloyd's k-means, reusing `SimilarityEngine`'s octave-aware BPM distance and plain energy distance (exposed as `internal` for this reuse) for cluster assignment. Two design points worth remembering:
  - **Centroid seeding is deterministic farthest-point sampling**, not random or evenly-spaced-by-sort. An earlier evenly-spaced-along-sorted-combined-score attempt failed its own test: with the library's near-duplicate track pairs sitting adjacent in sort order, two of the five seeds landed on JPB-High's two near-identical copies, starving Make Me Move/On & On of a centroid and merging them into one messy 4-member cluster instead of two clean pairs. Farthest-point seeding (each new seed is the candidate farthest from every seed chosen so far) fixed it immediately — caught by `ClusterEngineTest` before this ever reached a device.
  - **Octave-aware centroid averaging**: when recomputing a cluster's centroid BPM, each member's raw BPM is first folded to whichever octave (as-is/doubled/halved) sits closest to the *current* centroid before averaging — otherwise a cluster containing both a track and its octave-ambiguous double/half-time counterpart would average toward a meaningless midpoint tempo that matches neither.
  - `cluster()` filters out bpm/energy-null tracks internally (same "exclude, don't penalize" rule as `SimilarityEngine`) rather than trusting callers to pre-filter — the first version didn't, and its own null-safety test caught the resulting NPE immediately.
  - `nameFor()` buckets a cluster's centroid into thirds of the *candidate set's own observed BPM/energy range* (Slow/Mid-Tempo/Fast, Calm/Moderate Energy/High Energy), not fixed absolute thresholds — energy's absolute scale isn't validated beyond the narrow ~0.17-0.35 band real music has landed in so far (see the energy-extraction milestones above), so fixed thresholds tuned to a theoretical 0-1 range would have collapsed every real track into "Calm."
  - Covered by `app/src/test/java/.../ClusterEngineTest.kt`, a plain JVM unit test (new `testImplementation(libs.junit)` — this project previously only had `androidTestImplementation` junit) using the real bpm/energy values pulled from the on-device library as fixtures, so it doubles as a regression check against the actual library shape.
- **Schema**: `Playlist` gained `isGenerated: Boolean = false`. `AppDatabase` bumped to version 5 (still `fallbackToDestructiveMigration()`). `PlaylistDao` gained `insertPlaylistTracks` (bulk) and `deleteGeneratedPlaylists()`. `TrackDao` gained `getAllWithFeatures()` (suspend, one-shot, for batch jobs that don't need a live Flow). `ListenEventDao` gained `highCompletionCountsByTrack()` (`GROUP BY trackId WHERE completionPercent >= 80`) backing a new `TrackListenCount` projection.
- **`PlaylistRepository.regenerateGeneratedPlaylists(k = 5)`**: deletes all previously generated playlists, clusters the current library, names each cluster via `ClusterEngine.nameFor`, and orders each cluster's tracks by descending 80%+ listen count (ties broken by distance-to-centroid for determinism) before inserting — "favored/prioritized," not just included. Manual playlists are completely untouched (separate rows, `isGenerated = false`, no shared identifiers). Regenerates only when called — no observer/trigger wired to anything reactive. `k` defaults to 5 for real use but is a parameter (not hardcoded) since forcing a smaller k turned out to be the only practical way to test the listening-weight ordering on a library this small (see below).
- **Verified with `app/src/androidTest/java/.../PlaylistGeneratorVerificationTest.kt`** (Room needs a real `Context`, so this part is androidTest, not JVM-only):
  - Installing this milestone's schema bump destructively wiped `tracks`/`playlists`/`track_audio_features` on first DB open (expected, no real migration exists pre-release) but left the actual copied audio files sitting in `filesDir/music/` untouched. Added a `@Before` step that reconstructs the library from those files (grouping by file size to dedupe the several batch-import-era copies of each track down to one, then running them back through the real `MusicRepository.importTracks` pipeline) rather than needing to redo the SAF picker flow or re-download anything — genuinely useful going forward, since this is at least the third time in this project's history a schema bump has wiped the library.
  - **Result against the real 5-track library** (bpm/energy re-measured identically to the earlier 3-window-round values, confirming the analysis pipeline is deterministic across separate runs): with the production default `k=5` and exactly 5 distinct tracks, every cluster comes out as a **singleton** — `Calm / Fast` (Make Me Move, 190.48 bpm/0.215 energy), `High Energy / Slow` (Sky High, 127.66/0.350), `Moderate Energy / Fast` (On & On, 173.63/0.270), `Moderate Energy / Mid-Tempo` (JPB-High, 150.0/0.295), `Moderate Energy / Slow` (Firefly, 130.43/0.264). **This is a real, honest limitation to flag, not a bug**: with a fixed k equal to the number of distinct tracks, clustering degenerates to one playlist per track — it only starts doing real grouping once the library has meaningfully more tracks than clusters. Worth reconsidering `k` as `min(5, distinctTrackCount / 2)` or similar once the library is big enough to test that shape, but not changed now since it wasn't asked for.
  - **Genre-plausibility sanity check** on the singleton run: names are individually coherent given each track's own established measurements (e.g. Sky High correctly comes out "High Energy" — it's been the highest-energy track in every energy-extraction round so far) but can't demonstrate cross-genre *grouping* sensibility with only singletons. Re-ran clustering directly (not through the repository) at smaller k during design/testing and confirmed the two prog-house tracks (Firefly, Sky High) and the two bass-heavy tracks (JPB-High trap, On & On drumstep) land closer to each other than either does to Make Me Move (dance pop) — consistent with the similarity-engine milestone's earlier finding on the same tracks.
  - **Listening-history weighting, verified directly**: since production `k=5` gives no multi-track cluster to test ordering within, forced `k=2` (via the same real `regenerateGeneratedPlaylists` call, not a separate code path) to get genuine multi-track clusters. Cluster `Calm / Fast` came out as `[Make Me Move, On & On]`. Inserted two synthetic 80%+ `ListenEvent` rows directly for On & On (the event-recording pipeline itself was already verified against real playback in the listening-tracker milestone; this only checks that the *generator* reads and applies the counts), regenerated again, and confirmed On & On moved from last to first: `[On & On, Make Me Move]`. `assertEquals` on the resulting position, not just eyeballed.
  - No crashes; full existing androidTest suite (this + the TarsosDSP spike) still green after the schema bump.
- **Not done, deliberately, per instructions**: no UI surfaces generated playlists yet — no "Regenerate" button, no visual distinction from manual playlists in `PlaylistsScreen`. Reporting before touching any of that.

### k now scales with library size instead of a fixed constant (2026-08-29, done and re-verified)

The fixed `k=5` above degenerates to one singleton playlist per track once the library is small (exactly the 5-track case just verified) — real, not hypothetical, since it's the actual current library. Per instructions, replaced the fixed constant with `PlaylistRepository.computeClusterCount(candidateCount) = (candidateCount / 20).coerceIn(2, 5)` — one cluster per 20 tracks, clamped between 2 and 5 at both ends, so it degrades to genuine (if small) groupings at small sizes instead of singletons, and caps at the original ~5 for large libraries. `regenerateGeneratedPlaylists(k: Int? = null)` now computes this automatically when `k` isn't passed explicitly; tests can still force a specific `k` (used for the k=2 listening-weight test above, which no longer needs to override anything since auto-scaling now picks k=2 for this exact library size anyway — kept the parameter since forcing a shape is still occasionally useful for testing). No schema change involved in this round - pure repository logic, nothing destructive to flag.

**Re-verified on-device against the real 5-track library** (same tracks/values as above): auto-computed `k = (5/20).coerceIn(2,5) = 2`, producing two genuine multi-track clusters instead of five singletons:
- **`Calm / Fast`**: On & On (drumstep, 173.63 bpm/0.270 energy) + Make Me Move (dance pop, 190.48/0.215) — the two highest-bpm, lowest-energy tracks pairing together.
- **`Moderate Energy / Slow`**: Firefly (prog house, 130.43/0.264) + JPB-High (trap, 150.0/0.295) + Sky High (prog house, 127.66/0.350) — the three lower-bpm, higher-energy tracks.

JPB-High (trap) landing with the prog-house pair rather than On & On (drumstep) looks surprising at first glance given the earlier similarity-engine milestone's genre intuition, so checked the actual distances rather than assuming it's wrong: JPB-High→On&On = 0.0804, JPB-High→Firefly = 0.0807 — a 0.4% difference, a genuine near-tie already flagged in the similarity-engine milestone's report on these same three tracks, not a new bug. Which side of a near-tie a point lands on is legitimately sensitive to exact centroid movement during Lloyd's iterations.

Listening-weight test unaffected (still passes with an explicit `k=2` override, matching what auto-scaling now produces anyway by default). Full androidTest suite (6 tests: this file + the TarsosDSP spike) green; no crashes.

### AI playlists wired into the UI (2026-08-29, done and verified — no schema change, nothing to flag)

- **`PlaylistsScreen`**: a `Refresh` icon in the top bar (next to the existing `+`) calls `PlaylistViewModel.regenerateGeneratedPlaylists()`; disabled while a regeneration is in flight, backed by a new `isRegenerating` `StateFlow` following the same pattern as `LibraryViewModel.isImporting` (a `LinearProgressIndicator` shows underneath the top bar during that window). Each list row's `ListItem` now renders a small "AI" badge (`Surface` + `RoundedCornerShape(4.dp)`, `secondaryContainer` color) next to the name whenever `playlist.isGenerated` is true — chose a small inline badge over a separate "AI-Generated" section header, since the request's own example ("a small icon or label") pointed at a per-row marker and the existing list is a single flat `LazyColumn` with no grouping infrastructure to add just for this. No new dependency: used `Icons.Default.Refresh` from the already-present `material-icons-core` artifact rather than pulling in the extended icon set for something like a sparkle/auto-awesome icon.
- **`PlaylistViewModel.regenerateGeneratedPlaylists()`**: thin wrapper over the repository call already built and verified in the previous two rounds; no new logic beyond the loading flag.
- **Verified on-device with screenshots** against the real 5-track library (same tracks as the k-scaling re-verification above):
  - Playlists screen shows `Calm / Fast` and `Moderate Energy / Slow`, both tagged with the AI badge.
  - Created a manual playlist ("My Mix") and confirmed it renders with **no** badge, sitting in the same list — the visual distinction is real, not just "generated playlists happen to look different because they're populated."
  - Tapped Refresh and confirmed via a direct DB pull that regeneration actually re-ran (fresh playlist ids 36/37 replacing whatever existed before, fresh `dateCreated` matching the tap time) while "My Mix" (id 35, `isGenerated=0`) was left completely untouched — regeneration only ever touches `isGenerated=1` rows.
  - Opened the `Calm / Fast` AI playlist: renders identically to a manual playlist (same `PlaylistDetailScreen`, Rename/Delete/Add/Up/Down/Remove all present and functional) with tracks in the listening-weight order established earlier (On & On, boosted from an earlier test round, listed before Make Me Move). Tapped the first track and confirmed via `dumpsys media_session` that it actually plays through the normal queue/playback pipeline — no special-casing was needed anywhere outside `PlaylistsScreen` itself, since generated playlists are just rows in the same tables.
  - No crashes.
- **Known, accepted limitation, not fixed this round**: nothing stops a user from renaming/adding/removing tracks on an AI-generated playlist through the normal detail screen, and a subsequent Regenerate will silently blow those edits away (regeneration deletes and recreates every `isGenerated=1` row unconditionally). Not addressed now since it wasn't asked for; worth a "convert to manual" or "protect from regeneration" affordance later if it turns out to matter in practice.

### "Promote to manual" action (2026-08-29, done and verified — no schema change)

Addressed the limitation flagged directly above, per instructions.

- **`PlaylistDao.promoteToManual(playlistId)`**: a single-column `UPDATE playlists SET isGenerated = 0 WHERE id = :playlistId` — no need to round-trip the whole row through `@Update`. `PlaylistRepository.promoteToManual(playlist)` wraps it; `PlaylistViewModel.promoteSelectedPlaylistToManual()` calls it and updates the local `_selectedPlaylist` state with `.copy(isGenerated = false)` so the UI reflects it immediately without waiting on the `Flow` to re-emit.
- **UI**: a "Promote to manual" `TextButton` in `PlaylistDetailScreen`'s top bar, shown only when `playlist?.isGenerated == true` (same conditional-visibility idiom as the "More like this" menu item on tracks missing bpm/energy — hide the action entirely rather than show-and-disable it). Placed in the same top bar as Rename/Delete per the request, since those aren't in a dropdown menu in this screen to begin with.
- **Verified on-device** against the real library (`Calm / Fast` id 37, `Moderate Energy / Slow` id 36, `My Mix` id 35 manual):
  - Opened `Calm / Fast`, tapped Promote to manual. Confirmed via DB pull that id 37 flipped to `isGenerated = 0` with its two tracks (`On & On`, `Make Me Move`) and their order completely unchanged. The action button itself disappeared from the top bar immediately (only "Rename" remained) - confirms the local state update, not just the DB write.
  - Back on the Playlists list, `Calm / Fast` correctly showed with **no** AI badge.
  - Tapped Regenerate. DB pull afterward: **id 37 (promoted `Calm / Fast`) is completely untouched** - same id, same `dateCreated`, same track rows - while `Moderate Energy / Slow` was deleted and recreated as a new id with a fresh timestamp, refreshing normally. The current library also still clusters into a `Calm / Fast`-named group, so a **second, independent** AI-tagged `Calm / Fast` playlist was created alongside the promoted manual one - two playlists can legitimately share a name now (one manual, one AI), which is expected and harmless since they're just separate rows with no uniqueness constraint on `name`, not a bug.
  - No crashes.
- **Known, low-priority, not fixed**: a promoted playlist's auto-generated name (e.g. `Calm / Fast`) can collide with a freshly regenerated AI playlist that clusters to the same name later, so the list can show two identically-named playlists distinguished only by the AI badge. Left as-is per instructions - purely cosmetic (no id/data collision, both playlists function normally), and not worth solving speculatively before seeing whether it's actually confusing once the real library (not just these 5 test tracks) is imported. Revisit then if it turns out to matter - options include suffixing a promoted playlist's name at promotion time, or excluding names already in use by a manual playlist when naming new clusters.

## Git (2026-08-29)

Project is now under its own git repo at `C:\Users\Ido124\AndroidStudioProjects\local-music-player\.git` (previously the only git repo touching this code was the user's entire home directory, uncommitted — no real version control). `.gitignore` extended beyond the Android Studio default to actually anchor correctly (`/build` → `build/`, so `app/build/` is now covered too) plus `.kotlin/`, `*.apk`, `*.log`. Initial commit made with the full project as of the energy-in-UI milestone (source, Gradle wrapper, `CLAUDE.md`, the `androidTest` WAV fixtures) — build outputs, `.gradle/`, `.idea/`, `.kotlin/`, and `local.properties` correctly excluded from the commit.

## Visual polish pass before first phone install (2026-08-29, debug build verified with screenshots — release build not yet cut, per instructions)

- **Display name**: `strings.xml`'s `app_name` changed to "Nera Music" (was "NERA Music Player"). Package name `com.nera.musicplayer` deliberately untouched, per instructions, to avoid a reinstall on the phone. `HomeScreen`'s top bar title was hardcoded to the old literal string separately from `app_name` - switched it to `stringResource(R.string.app_name)` so the two can't drift again.
- **Theme (`ui/theme/Color.kt` + `Theme.kt`, both rewritten)**: previously `Theme.kt` was literally just unmodified `darkColorScheme()`/`lightColorScheme()` defaults - zero customization since the project was scaffolded. New design:
  - A fixed brand accent trio - vivid purple (`#B026FF`, primary), cyan (`#22F0FF`, secondary), magenta (`#FF2E9F`, tertiary) - plus a dark, faintly-purple-tinted base (`#0E0A14` background / `#15101E` surface) rather than neutral gray, per "dark base" in the brief.
  - **Dynamic color (Material You) on API 31+, with the brand accents layered back on top**: `dynamicDarkColorScheme`/`dynamicLightColorScheme` supplies the neutral background/surface/outline tokens (so the app still feels tied to the phone's actual wallpaper), but `primary`/`secondary`/`tertiary` and their container/on-* pairs are always overridden back to the fixed brand trio via `.copy(...)` - otherwise a wallpaper with, say, green tones would silently replace the purple/cyan/magenta identity the brief specifically asked for. Below API 31, falls back to the fully custom `NeraDarkColors`/`NeraLightColors` schemes.
  - Still respects system light/dark (`isSystemInDarkTheme()` default, unchanged) rather than forcing dark always - "dark base" was read as describing the flagship aesthetic (and the primary thing screenshotted for approval), not as "ignore the user's system setting."
  - `themes.xml` (the pre-Compose View-system theme, only visible for a frame before Compose content loads) updated from stock `Theme.Material.Light.NoActionBar` with no color overrides to a dark `windowBackground`/`statusBarColor`/`navigationBarColor` matching the new base, so a cold start doesn't flash white first.
- **App icon**: project had **no launcher icon at all** before this - no `android:icon` in the manifest, no mipmap/drawable resources, so it had been running with whatever generic placeholder Android substitutes. Built a standard adaptive icon (min SDK is already 26, i.e. API 26+, so no legacy PNG fallback needed - `mipmap-anydpi-v26` alone covers every supported device): `drawable/ic_launcher_background.xml` (flat dark `#14091F`), `drawable/ic_launcher_foreground.xml` (a 5-bar equalizer/waveform mark, sharp rectangles, purple→magenta→cyan linear gradient fill, kept within the adaptive-icon safe zone), combined via `mipmap-anydpi-v26/ic_launcher.xml`/`ic_launcher_round.xml`. `AndroidManifest.xml`'s `<application>` gained `android:icon`/`android:roundIcon` referencing them. Geometric/vector only, no raster image, per instructions.
- **`PlaybackBar` rewritten** - previously five plain `TextButton`s reading literal text ("Prev", "Play", "Shuffle: Off", etc.), the clearest "leftover default Compose styling" spot in the app. Now: real `Icons.Filled` glyphs (`Shuffle`/`SkipPrevious`/`PlayArrow`or`Pause`/`SkipNext`/`Repeat`or`RepeatOne`) - none of these exist in the `material-icons-core` artifact already in use, so added `material-icons-extended` (version pinned by the existing `compose-bom`, no version-skew risk). Play/pause elevated as a filled `primary`-colored circle (the one "big bold" element, everything else deliberately smaller/muted) rather than all six controls reading as equally weighted; shuffle/repeat tint `secondary` (cyan) only when active, `onSurfaceVariant` (muted) when off - color communicates state instead of text swapping between "Shuffle: On"/"Shuffle: Off". Bar itself wrapped in a `surfaceVariant` `Surface` with elevation so it visually separates from the list above it (previously an unstyled `Surface {}` that blended into the background).
- **Badges recolored with intentional semantics** rather than default `onSurface` text color: `HomeScreen`'s BPM badge → `secondary` (cyan), Energy badge → `tertiary` (magenta); `PlaylistsScreen`'s "AI" badge → `primaryContainer` (purple) - a distinct third hue from the two data badges, since it's a feature marker rather than a data value.
- **Verified on-device with screenshots**, system UI mode forced to dark (`adb shell cmd uimode night yes`) to capture the flagship look: Home (library list, BPM/Energy badges, playback bar both idle and mid-playback), Playlists (AI vs. manual badge contrast), Playlist Detail (Up/Down/Remove/Rename/Delete, active playback progress), and the app drawer entry itself (confirms "Nera Music" label + the equalizer icon rendering correctly in the launcher's circular mask, standing out clearly against the stock Google icon set). No crashes across the pass.
- **Not done yet, deliberately**: no release build has been cut. This was a debug-build-and-screenshot pass only, per instructions to show results before building the final release version.

## First signed release build (2026-08-29, done and verified)

- `app/build.gradle.kts` gained a conditional `signingConfigs { release { ... } }` block reading `keystore.properties` (loaded via `java.util.Properties`, only if the file exists — keeps a debug-only checkout, e.g. fresh CI, from failing at Gradle configuration time), and wired it onto `buildTypes.release` the same way, conditionally.
- `keystore.properties` (storeFile/storePassword/keyAlias/keyPassword) and `keystore/nera-music-release.jks` are both new, both gitignored (`.gitignore` gained `/keystore.properties`, `/keystore/`, `*.jks`, `*.keystore`) — the signing key and its credentials must never reach version control.
- `./gradlew assembleRelease` — **BUILD SUCCESSFUL**, `app/build/outputs/apk/release/app-release.apk` (13 MB).
- **Verified the signature directly**, not just trusted a clean build: `apksigner verify --verbose --print-certs` on the output APK confirms `Verifies` / `Verified using v2 scheme: true`, signer CN=Nera Music, RSA 2048 — actually signed with the real release key, not an unsigned or debug-signed artifact.
- Not done yet: not installed/tested on-device as a release build, not published anywhere.

## Two bugs found during visual-polish re-verification, fixed (2026-08-29, done and verified on-device)

- **UUID showing as a track title.** Root cause: `PlaylistGeneratorVerificationTest`'s `@Before` reconstructs the library from the raw `.mp3` files already sitting in `filesDir/music/` by calling `MusicRepository.importTracks(Uri.fromFile(file))`. `importTracks`'s display-name fallback chain is `DocumentFile.fromSingleUri(context, sourceUri)?.name ?: sourceUri.lastPathSegment ?: "track"` - `DocumentFile.fromSingleUri` only resolves real SAF `content://` document URIs, so for a `file://` URI it returns null, falling through to `lastPathSegment` - which for one of our own copies is the UUID filename Room never meant to surface. That collided with one specific track (the Sky High copy) that also has no embedded ID3 title tag to fall back to first, so the raw UUID string reached the UI as the title.
  - **Fix, in production code, not just the test**: added a `UUID_FILENAME_REGEX` guard in `MusicRepository.importTracks` - if the derived fallback name is UUID-shaped, it's discarded in favor of a final `"Unknown Track"` fallback rather than trusting it as a real name. Fixed at this layer (not by patching the test helper) since any caller feeding a URI whose display name can't be resolved should get the same protection, not just this one androidTest path.
  - Existing bad row (already inserted before this fix shipped) doesn't self-heal from a code change alone, since the reconstruction only runs when the library is empty - corrected it directly via a DB pull/patch/push (`nera_music.db`, WAL checkpointed and folded in first) rather than wiping and losing the other four real tracks.
  - **Verified on-device**: fresh screenshot of the Home library list shows "Unknown Track" in place of the old UUID string, everything else (titles, BPM/Energy badges) unaffected.
- **Playlist Detail top bar overcrowding.** Root cause: the top bar packed a title plus up to four actions as literal `TextButton`s (`Back`, conditionally `Promote to manual`, `Rename`, `Delete`) plus an `IconButton` - all inline, no wrapping, so a long playlist name plus "Promote to manual" text visibly crowded/overlapped on real screen widths. This was a real layout defect, not just a style mismatch with the rest of the app.
  - **Fix**: converted the leading action to a proper icon `IconButton` (`Icons.AutoMirrored.Filled.ArrowBack`) matching the nav-icon idiom used everywhere else in the app, kept "Add tracks" as a direct `IconButton` since it's the most common action, and moved the other three (conditionally-shown "Promote to manual", "Rename", "Delete") into a `DropdownMenu` behind a `MoreVert` overflow icon - the same overflow-menu idiom `HomeScreen`'s `TrackRow` already uses for its long-press track menu, so this isn't a new pattern for the codebase. The title `Text` now has room to render in full even for long AI-generated names like "Moderate Energy / Slow" without crowding the icons.
  - **Verified on-device**: screenshotted the detail screen for `Moderate Energy / Slow` (one of the longer AI-generated names) - title renders fully, back arrow / add / overflow icons are cleanly spaced with no overlap. Opened the overflow menu and confirmed all three actions render correctly ("Promote to manual" present since this playlist is AI-generated). No crashes.

## Search, By Name sort, light theme, playback time counter (2026-08-30, done and verified on-device)

- **Search bar (`HomeScreen`)**: an always-visible `OutlinedTextField` above the track list, filtering by title or artist substring, case-insensitive. `LibraryViewModel` filters the already-sorted `TrackWithFeatures` list in memory (`combine(sortedTracks, searchQuery) { list, query -> list.filter { ... } }`) rather than adding a Room `LIKE` query - a substring scan over a few hundred tracks is well under a frame budget, so this stays snappy without a debounce. `HomeScreen` now distinguishes "library is genuinely empty" (raw `tracks` flow, unaffected by search) from "search matched nothing" (filtered `tracksWithFeatures` empty but raw library isn't) so the empty-state message doesn't lie to the user; the raw `tracks` flow used for this check is the same one the Playlist "Add tracks" dialog already reads, so that dialog stays unaffected by whatever's typed into Home's search box.
- **"By Name" sort**: `LibrarySortOrder` gained `BY_NAME`; new `TrackDao.observeAllWithFeaturesByTitle()` (`ORDER BY tracks.title COLLATE NOCASE ASC` - case-insensitive alphabetical, so "apple" sorts before "Banana" rather than every capital letter first). Cycle order is now Date → BPM → Energy → By Name → Date, per instructions.
- **Light/low-contrast theme, persisted (`ui/theme/ThemePreferences.kt`, `ui/SettingsViewModel.kt`, `ui/SettingsScreen.kt`, new)**:
  - `AppTheme { DARK, LIGHT }` persisted via plain `SharedPreferences` (no DataStore dependency needed for one enum) - `ThemePreferences` exposes it as a `StateFlow`, `SettingsViewModel` wraps it, `MainActivity` collects it and feeds `NeraMusicPlayerTheme(darkTheme = ...)` directly rather than the old `isSystemInDarkTheme()` default - the Settings choice is now the sole source of truth for which scheme renders, not the system setting.
  - `dynamicColor` is explicitly forced `false` at this call site: the light theme is a deliberately curated low-contrast look (soft lavender-white surfaces, dark plum-gray text instead of pure black - see `NeraLightBackground`/`NeraLightSurface`/`NeraLightOnSurface` in `Color.kt`), not whatever Material You would derive from the device wallpaper. Same purple/cyan/magenta accent trio as dark, just with `background`/`surface`/`surfaceVariant`/`onSurface`/`onSurfaceVariant` overridden with the low-contrast light values instead of Material's stark default white/black.
  - `MainActivity` also flips `isAppearanceLightStatusBars`/`isAppearanceLightNavigationBars` (via `WindowCompat`) when the light theme is active - without this the status bar icons stay white-on-white-ish and become unreadable, a real functional gap the theme switch would otherwise introduce.
  - **Settings entry point**: new gear-less "Settings" item in `HomeScreen`'s overflow menu (see below) opening `SettingsScreen` - two `RadioButton` rows, Dark/Light, applies immediately on tap.
  - `HomeScreen`'s top bar was restructured to avoid recreating the overcrowding bug fixed above: "Sort: X" and the import `+` stay as direct actions (most frequent), while "Playlists" and the new "Settings" moved into a `MoreVert` overflow `DropdownMenu` - same idiom as `PlaylistDetailScreen`'s overflow menu.
- **Playback time counter (`PlaybackBar`)**: `PlayerViewModel.uiState` already carried a live `positionMs`/`durationMs` (a 500ms ticker was already running for listen-tracking), so this was UI-only - a `formatMs(Long): String` helper (`m:ss`) and a `"$position / $duration"` label placed in a `Row` alongside the track title (title gets `Modifier.weight(1f)` + ellipsis so a long title doesn't push the counter off-screen).
- **Verified on-device** (emulator, since the connected phone already had a different signed build installed - see below): imported a batch of real/synthetic tracks with deliberately varied titles/artists, then:
  - Search: typed "high" → matched "JPB - High" and "sky_high_ncs" only, excluding everything else; typed "karra" → matched both "Make Me Move" rows via the **artist** field (Culture Code, KARRA), confirming the match isn't title-only. Case-insensitive in both cases (lowercase query against mixed-case data).
  - Sort: cycled to "Sort: Name" - list re-ordered alphabetically (Cartoon → Firefly → JPB-High → Make Me Move → sky_high_ncs → test_tone), case-insensitive.
  - Theme: selected Light in Settings - applied instantly (no restart needed), status bar icons flipped to dark and stayed legible, accent colors (BPM cyan / Energy magenta / playback purple) unchanged from dark theme, background/surfaces visibly low-contrast rather than stark white. Force-stopped and relaunched the app - still Light, confirming persistence.
  - Time counter: started playback, screenshotted at "0:02 / 3:12", waited, screenshotted again at "0:22 / 3:12" with the slider visibly advanced - confirms live updates during playback, not just a static value at tap time.
  - No crashes across the pass.
- **Not done on the phone during this verification pass**: the connected physical phone already had a differently-signed build installed (from the other Claude session's earlier work) that conflicted with the debug signature, so this round's on-device verification used the emulator instead, to avoid uninstalling (and losing local data on) the user's actual phone without checking first. The phone gets the new signed release build in the next step instead.

## Real music batch (2026-08-30): NCS/TheFatRat download, phone import, sourcing checklist update

- Worked from a user-provided `music_sourcing_checklist.md` (a 320-track YouTube-library reconstruction list, categorized into explicitly-free/high-confidence-free/Monstercat/mainstream/independent/nightcore) - processed the two free-download categories per instructions, verifying every source live rather than trusting the checklist's own categorization.
- **9 tracks downloaded** from first-party sources only: 7 from `ncs.io` (found via the real `Download Track` link on each track page, `/track/download/<uuid>`, header-verified `audio/mpeg` before saving) plus TheFatRat's own site (`thefatrat.com/release/warbringer`, MP3 hosted on his own Wix CDN, gated only by his Terms of Use). Imported all 9 into the on-device library via the real SAF import flow (not a DB-level insert), so BPM/Energy analysis ran normally on each.
- **Two corrections applied against the checklist itself, not just followed blindly**: (1) the checklist's "Warriyo - Mortals (ELPORT remix)" doesn't exist among NCS's 8 official Warriyo/Mortals releases - substituted the official original after confirming with the user, same pattern as the earlier Tobu-Hope swap. (2) "TheFatRat, Slaydit & Anjulie - Stronger" was filed under "high-confidence free-download artists" despite its own `[Monstercat Release]` tag - confirmed via monstercat.com/release/MCS862 and its absence from TheFatRat's own downloads page that it's a commercial Monstercat release, and moved it to the Monstercat category in the updated checklist instead of downloading it as free.
- **NEFFEX's 9 tracks deliberately left undownloaded**: confirmed via his official SoundCloud (soundcloud.com/neffexmusic) that all 9 are genuine, currently-live `[Copyright Free]` releases, but every track's "Free Download" button routes through `gate.sc` (a link-gate/ad-interstitial redirector) to a third-party site, `tunebula.com`, that doesn't clear the same first-party bar as ncs.io/thefatrat.com. Flagged in the tracking file for the user to grab manually rather than piping an automated download through an ad-gate.
- Minor import cleanup: a mid-batch coordinate-scaling mistake during the SAF picker automation caused one accidental duplicate ("Don't Surrender" imported twice) and one missed track (Game Over); both fixed via the app's own "Remove from library"/"Delete file" actions afterward - no leftover bad data. Four tracks' titles fell back to the raw pushed filename (no embedded ID3 title tag on those specific masters, e.g. Rise Up and Warbringer) - fixed by renaming the source files to the correct `Artist - Title [NCS Release]`-style name and re-importing, rather than leaving underscore-heavy filenames as permanent library titles.
- **Updated `music_sourcing_checklist.md`** (sent back to the user, saved in place at the original path): new "Downloaded and imported" section listing all 9 with sources; "Still needed" restructured into the NEFFEX manual-download callout plus the original Monstercat/mainstream/independent/nightcore categories left exactly as before (with Stronger's move noted inline).
- Verification note: the phone's own Nera Music install was mid-batch when a live user session on the phone (unrelated Chrome browsing) got briefly, accidentally interacted with by an automation tap aimed at the wrong screen coordinate - caught immediately, no further taps sent, confirmed harmless with the user.

## Settings toggle: show/hide track analysis badges (2026-08-30, done and verified on-device)

- **`LibraryDisplayPreferences`** (new, same pattern as `ThemePreferences`): a single persisted boolean (`show_analysis_badges`, default `true`) via plain `SharedPreferences`, reusing the same `nera_settings` prefs file. Wired into `SettingsViewModel` alongside the existing theme state.
- **`SettingsScreen`**: new "Library" section below the theme picker with a `Switch` labeled "Show track analysis badges".
- **`HomeScreen`/`TrackRow`**: `HomeScreen` now takes a `settingsViewModel` param (passed down from `NeraApp`, same instance the Settings screen reads/writes since both resolve through the same Activity-scoped `viewModel()` call) and collects `showAnalysisBadges`, threading it into `TrackRow` as a new `showBadges` param. `trailingContent` is gated on `showBadges && (bpm != null || energy != null)` instead of just the null checks - purely a rendering condition, so BPM/Energy sort and "More like this" (which still checks the raw `bpm`/`energy` values, not the display flag) stay fully functional with badges hidden, per instructions.
- **Verified on-device** (emulator, existing library with mixed BPM/Energy-present and null-feature tracks): toggle defaults on (badges visible, matches pre-existing behavior - no regression). Switched off - all BPM/Energy badges disappeared from every row instantly, titles/subtitles unaffected, no layout artifacts. Confirmed the display-only scope: cycled Sort to BPM while badges were hidden and the list still re-ordered correctly (test_tone/test_tone_2 sank to the bottom as before) - the underlying feature keeps working with only the badge rendering suppressed. Force-stopped and relaunched the app - toggle still off, confirming persistence; switched back on and confirmed the library's actual track data was untouched throughout (a transient empty-list frame right after process restart, from Room's StateFlow not yet re-emitting, was the only hiccup - same harmless artifact seen after the light-theme persistence check earlier, not a regression from this change).

## Folder-based import + duplicate detection (2026-08-30, done and verified on-device)

- **`MusicRepository`**: the old inline per-file logic in `importTracks` was extracted into a shared private `importOne(sourceUri, originalName, existingKeys)` so both the existing multi-file SAF picker and the new folder scan go through the same copy/tag/dedupe/insert/analyze path - no duplicated logic between the two entry points.
- **`importFromTree(treeUri, onProgress)`** (new): takes the `Uri` from SAF's `OpenDocumentTree`, recursively walks it via `DocumentFile.listFiles()` (`collectAudioFiles` - recurses into every subdirectory, filters files by `audio/*` MIME type or a fallback extension allowlist for providers that report a generic type), then imports every audio file found in one pass. `onProgress(current, total)` fires after each file so the UI can drive a determinate progress bar across what can be 100+ files - the total is known upfront since the whole tree is walked (cheap, metadata-only) before any file is copied/decoded.
- **Duplicate detection**: `importOne` checks a case-insensitive `title|artist` key against a `Set` loaded once per batch (`TrackDao.getAllTitleArtists()`, a lightweight two-column projection - not a full `Track` fetch) before inserting; a match deletes the just-copied file and reports a skip instead. Chose title+artist over a file hash: it catches what actually annoys a user (the same song showing up twice, however differently it was re-encoded/re-tagged/renamed along the way) - a byte-exact hash would miss that entirely and would also be wrong for e.g. a legitimately-remastered re-rip. Applies to both import paths (folder scan and the original multi-file picker), not just the new one.
- **`LibraryViewModel`**: new `importFolder(treeUri)`, `importProgress: StateFlow<Pair<Int,Int>?>` (current, total; `null` when idle), and `lastImportSummary: StateFlow<ImportSummary?>` (`ImportSummary(imported, skippedDuplicates)`, cleared via `clearImportSummary()`).
- **`HomeScreen`**: "Import folder" added to the overflow menu (via `ActivityResultContracts.OpenDocumentTree()`) alongside Playlists/Settings, since it's a bulk/occasional action like those. While importing, the plain indeterminate bar is replaced with a determinate one plus an "Importing X / Y…" label once the total is known (briefly indeterminate during the initial tree walk). An `AlertDialog` reports the final "Imported N track(s). Skipped M duplicate(s)." summary, dismissed via `clearImportSummary()`.
- **Verified on-device** (emulator) with a real MusicBrainz Picard-style nested tree (`PicardTest/ArtistA/AlbumX/{Song One,Song Two}.wav`, `PicardTest/ArtistA/AlbumY/Song Three.wav`, `PicardTest/ArtistB/AlbumZ/{Song Four,Song One}.wav` - "Song One" deliberately duplicated across two different artist/album folders, same untagged file so both share the same fallback title with no artist):
  - Picked `PicardTest` as the root via the real SAF folder picker (not a shortcut/mock) - the app correctly requested and received folder access, then found all 5 files across both nested artist/album subtrees.
  - Progress bar visibly advanced across the batch (screenshotted mid-import) rather than the UI freezing or showing only a static spinner - each file's BPM/energy analysis still ran inline like any other import.
  - Final dialog: **"Imported 4 tracks. Skipped 1 duplicate."** - exactly right. Searching the library afterward confirmed Song One/Two/Three/Four each appear exactly once - the cross-folder duplicate was silently skipped, not double-imported, and the other three genuinely-distinct tracks (despite sharing the same untagged-fallback-title mechanism) were not falsely flagged as duplicates of each other.
  - No crashes, no ANRs, across the scan+import+dialog pass.
- Also verified this session didn't regress anything: existing library (from prior milestones) survived two full emulator process kills (one accidental, from an adb-daemon restart killing the emulator process entirely mid-session) with app data intact both times, confirming the disk-image-persists-across-cold-boot behavior documented earlier in this file still holds.

## No-picker drop-folder import, bypassing a broken device file picker (2026-09-01, done and verified on-device)

- **Why**: the user's actual phone (a POCO X6 Pro 5G) has a MIUI-customized `com.google.android.documentsui` whose `ACTION_OPEN_DOCUMENT_TREE` flow has no discoverable "confirm this folder" action anywhere - not in grid view, list view, the breadcrumb, the nav drawer, the overflow menu, long-press-to-select, or a hardware ENTER key, confirmed via two full accessibility-tree dumps across a leaf folder, a folder-of-folders, and the root. The recursive scan code itself was already proven correct on the emulator's stock picker (see previous section); this is purely a device-specific picker UI gap, worked around by removing the picker from the flow entirely.
- **`MusicRepository.scanImportFolder()`** (new): scans a fixed drop folder - `Environment.getExternalStorageDirectory()/NeraMusicImport` - recursively via plain `java.io.File`, no SAF/DocumentFile involved, and imports through the same `importOne` dedup path as the other two flows.
  - This location (not app-private external storage) was chosen after confirming on-device that `adb push` can't create directories under `Android/data/com.nera.musicplayer/files/` on this phone (`secure_mkdirs() failed: Operation not permitted`) even though the app's own runtime access to that path is unrestricted - a plain top-level folder pushes fine everywhere tested this session.
  - Reading it back from the app requires `READ_MEDIA_AUDIO` (API 33+) / `READ_EXTERNAL_STORAGE` (below 33, both declared in the manifest) - scoped storage denies direct `File` reads of anything outside the app's own sandbox without it, regardless of how the file got there.
  - **Cleanup without delete permission**: `adb`-pushed files aren't owned by this app, so scoped storage denies `File.delete()` on them too - confirmed on-device via logging (`deleted=false`) even with the read permission held; deleting a non-owned file would need a `MediaStore` user-consent flow, exactly the kind of extra prompt this feature exists to avoid. Instead, a `SharedPreferences` fingerprint set (`path|size|lastModified`) tracks what's already been handled, so a rescan only processes genuinely new files - `file.delete()` is still attempted as a best-effort (harmless no-op where it's denied, real cleanup on setups where it isn't).
- **`LibraryViewModel.rescanLibrary(showEmptyResult)`**: reuses the same `isImporting`/`importProgress`/`lastImportSummary` state the folder-tree import already had, so the existing progress bar and summary dialog UI covers this path with no new UI plumbing. `showEmptyResult = false` for the automatic on-launch scan (an empty drop folder is the common case and shouldn't nag every startup); the manual "Rescan library" menu item always reports back, even 0/0.
- **`HomeScreen`**: a `LaunchedEffect(Unit)` triggers the scan once per app launch - if `READ_MEDIA_AUDIO`/`READ_EXTERNAL_STORAGE` isn't yet granted it requests it first (standard one-time system permission dialog, not SAF), then scans on grant. "Rescan library" added to the overflow menu alongside "Import folder" for on-demand re-scans after pushing more files, going through the same permission check.
- **Verified on-device (emulator)**: pushed a nested tree (same Artist/Album/Song shape as before) into `/sdcard/NeraMusicImport/`, including a cross-folder duplicate. Launch showed the real system permission dialog ("Allow Nera Music to access music and audio on this device?") on first run; granting it triggered the scan automatically - **"Imported 4 tracks. Skipped 1 duplicate."**, matching the tree-import result exactly. Relaunching afterward (leftover undeleted files still sitting in the drop folder, since delete is denied) produced **no** dialog and **no** log activity for those files at all - confirmed via `adb logcat` that the fingerprint filter excluded them before `importOne` was ever called, not just that they got re-detected as duplicates. Manually pushing one fresh file and using "Rescan library" correctly picked up only that new file.
- **Verified on the actual POCO X6 Pro 5G** (the phone whose picker started this whole workaround): pushed a real file to `/sdcard/NeraMusicImport/`, installed the signed release build, launched - the same system permission dialog appeared and worked correctly (unlike the SAF picker), and the scan ran automatically on grant: **"Imported 1 track. Skipped 0 duplicates."** Confirms the fix isn't emulator-only - it's device-independent by construction (plain `File` I/O + a standard runtime permission, no SAF/picker involved at any point).

## Real 174-track library import + investigation of two reported bugs, both fixed (2026-09-03, done and verified on-device)

- **Real library import**: full `pm clear com.nera.musicplayer` (wipes Room DB, internal files, SharedPreferences, and revokes granted runtime permissions - required explicit user approval since the Claude Code auto-mode classifier blocks `pm clear` by default) to remove everything from earlier manual/test imports, then the user's real 174-track PC library was pushed via `adb push` into `/sdcard/NeraMusicImport/` and picked up by the existing drop-folder scan. Final result matched expectations exactly: **"Imported 174 tracks. Skipped 159 duplicates."** (the 159 came from an unrelated mid-transfer retry after a wireless-adb drop caused `adb push` to nest the source folder a second time under the already-partially-pushed destination - harmless, since the recursive scanner and title+artist dedup both handle arbitrary nesting and re-pushed duplicates correctly by construction).
- **User then reported two apparent bugs** against this real library: "More like this" appearing to do nothing when tapped, and no AI-generated playlists showing up. Investigated both on-device before making any fix, per instructions:
  - **AI playlists**: not actually broken. `PlaylistRepository.regenerateGeneratedPlaylists()` was, and still is, never auto-triggered by any import path - confirmed by reading `PlaylistRepository`/`PlaylistsScreen`, and confirmed on-device that the empty state was showing because nothing had tapped "regenerate" since the `pm clear` wipe. Manually triggering it against the real 174-track library produced 5 well-distributed clusters with zero exceptions in logcat, proving the clustering code (`ClusterEngine`) handles the larger, more varied real dataset fine - no null-BPM/energy crash, no silent failure.
  - **"More like this"**: tap handler and gating are correct. `HomeScreen`'s `onMoreLikeThis` lambda is only non-null when `item.bpm != null && item.energy != null`, and the `DropdownMenuItem` itself is only rendered `if (onMoreLikeThis != null)` - so a track missing analysis simply doesn't show the option at all, it isn't a dead click. Live-tested twice on different real tracks with PID-filtered logcat capture (full MediaCodec/FLAC-or-vorbis decode sequence both times) plus before/after screenshots - both times the queue changed and playback started correctly, no exceptions.
  - **Real bug found along the way, not one of the two originally reported**: `PlayerViewModel`'s `trackTitle` (and `isPlaying`) were only ever set inside the `MediaController`'s `Player.Listener` callbacks (`onMediaItemTransition` / `onIsPlayingChanged`), never initialized from `controller.currentMediaItem` / `controller.isPlaying` at the point the `MediaController` first connects. So reconnecting to an already-playing session - e.g. the app's `Activity` (and thus its `AndroidViewModel`s) gets recreated while the `MediaSessionService` keeps running in the background - left the mini-player stuck on "No track loaded" with a frozen play icon, even though the position/duration ticker (polled independently) kept advancing correctly underneath. This is a very plausible explanation for "nothing visible happens" if the user hit "More like this" in that state and the queue genuinely changed but the title display just never caught up.
- **Fixes applied**:
  - `PlayerViewModel.kt`: the `init` block's first `_uiState` update (previously just `shuffleEnabled`/`repeatMode`) now also seeds `trackTitle` from `c.currentMediaItem?.mediaMetadata?.title` and `isPlaying` from `c.isPlaying`, right after the `MediaController` connects - before that, only a future transition/play-state event would ever populate them.
  - `LibraryViewModel.kt`: gained a `PlaylistRepository` instance and a `regenerateIfNewTracks(summary: ImportSummary)` helper (`if (summary.imported > 0) playlistRepository.regenerateGeneratedPlaylists()`), called after all three import paths (`importTracks`, `importFolder`, `rescanLibrary`) once their own `ImportSummary` is known - gated on `imported > 0` specifically so a scan that finds nothing new (the common case for the auto-scan-on-launch) doesn't recluster the whole library for no reason.
- **Verified on-device (POCO X6 Pro 5G, signed release build)**:
  - Title-on-reconnect: started playback ("סמי חופשי"), exited the app via the back button (finishes the `Activity`, confirmed via `dumpsys media_session` that the session stayed `active=true` and the process `pidof` stayed alive throughout - the `MediaSessionService` genuinely kept playing in the background), relaunched from the launcher, and screenshotted immediately - the mini-player showed the correct title and a live position (`0:20 / 5:22`) with the pause icon right away, no stale "No track loaded" frame.
  - Auto-regenerate-on-import: deleted all 5 existing AI playlists, copied a stock device ringtone (`Atlantis.ogg`, genuinely new title/artist so it can't dedupe-skip) into the drop folder, ran "Rescan library" (confirmed via logcat: `Analyzed track 175: bpm=... energy=...`, zero exceptions), then navigated straight to the Playlists screen **without** tapping the refresh icon - 5 AI playlists were already there. Cleaned up afterward: removed the test track via "Delete file" and re-ran regenerate once more so the real 174-track library's playlists reflect only real data, not the test file.

## Album art + full-screen Now Playing view (2026-09-03, done and verified on-device)

- **Preceded by a source-file check, not an assumption**: user asked whether the real library's files actually carry embedded artwork before building anything. Scanned all 174 files in the original `~/Music` source tree with `mutagen` (Python) - **all 174 have embedded art** (FLAC picture blocks), zero missing. Confirmed separately that the app itself had no album-art code at all (`MusicRepository`'s import path called `MediaMetadataRetriever.extractMetadata` for title/artist/album/genre/year/duration but never `getEmbeddedPicture()`; `Track` had no art column; no `NowPlaying`/`AlbumArt` file existed anywhere) - so this was a from-scratch feature, not a bug in an existing one.
- **Schema change, migration not destructive fallback**: `Track` gained `albumArtPath: String?` (a file path, not inline bytes - keeps rows small, lets an image loader cache/decode lazily instead of round-tripping through Room every recomposition). `AppDatabase` bumped `version = 5 -> 6` with an explicit `Migration(5, 6)` (`ALTER TABLE tracks ADD COLUMN albumArtPath TEXT`) via `.addMigrations(...)`, and **`.fallbackToDestructiveMigration()` was removed entirely**, not just skipped for this bump - the old code's comment ("pre-release schema, no user data to preserve yet") is no longer true now that the real 174-track library exists, so keeping a destructive fallback as a "safety net" would mean any *future* schema change that forgets an explicit migration silently wipes the user's library instead of crashing loudly during development. Crashing loudly is the correct failure mode from here on.
- **Backfill chosen over lazy-only, since the source files are still present**: every import path (`importOne` in `MusicRepository`) copies the source file into app-private storage (`filesDir/music/<uuid>.<ext>`) and never deletes it afterward - so all 174 already-imported tracks' original files are still sitting right there, re-openable without re-importing. `MusicRepository.backfillAlbumArtIfNeeded()` (new) does a one-time pass: for every track with `albumArtPath IS NULL`, reopens `MediaMetadataRetriever` on its own `uri`, extracts embedded art the same way a fresh import would, and updates just that row. Gated by a `SharedPreferences` flag (not a live `albumArtPath IS NULL` re-check) so a track whose file genuinely has no embedded picture doesn't get re-scanned on every launch forever - null is a valid, final outcome for those, not "not yet attempted". Triggered unconditionally from `LibraryViewModel`'s `init` block, so it runs automatically with no user action and no separate migration-time UI.
- **Extraction (`MusicRepository.extractAlbumArt`)**: reuses the same `MediaMetadataRetriever` instance already open for title/artist/etc. metadata (both at import time and during backfill) rather than opening a second one - calls `retriever.embeddedPicture`, and if non-null saves it to `filesDir/album_art/<uuid>.jpg`, storing the absolute path on the `Track` row. A track with no embedded picture gets `albumArtPath = null`, which is treated as a normal, expected state throughout, not an error.
- **Art flows through the same `MediaMetadata` mechanism title/artist already used**, rather than a separate lookup path: `Track.toMediaItem()` now calls `.setArtworkUri(Uri.fromFile(File(albumArtPath)))` when present. `PlayerUiState` gained `artist: String?` and `albumArtUri: Uri?`, both seeded in `PlayerViewModel`'s `MediaController`-connect block (the same place last session's trackTitle-on-reconnect fix lives) and updated in `onMediaItemTransition` - so reconnecting to an already-playing session now restores art/artist immediately too, not just the title. Free side effect: since Media3's system notification reads `MediaMetadata.artworkUri` automatically, the lock-screen/notification media controls now show real album art as well, with no extra code for that.
- **`NowPlayingScreen.kt`** (new): full-screen view - a large (`aspectRatio(1f)`) art frame with `Coil`'s `AsyncImage` (new dependency, `coil-compose` - no image-loading library existed in the project before this), title, artist, seek bar + time counter, and the same transport row as the compact `PlaybackBar` (shuffle/prev/play-pause/next/repeat) at a larger size - `formatMs`/`repeatLabel` were changed from `private` to file-visible in `PlaybackBar.kt` so both screens share one implementation instead of duplicating the time-format/repeat-label logic. When `albumArtUri` is null, the art frame renders a `MusicNote` icon on `primaryContainer` instead - a deliberate themed placeholder (same accent-color system as the rest of the app), not a generic gray box or a broken-image icon.
- **Navigation**: `NeraApp`'s manual `Screen` sealed class gained `NowPlaying`. Tapping a track row in either `HomeScreen`'s library list or `PlaylistDetailScreen`'s track list now calls `playerViewModel.playQueue(...)` **and** a new `onOpenNowPlaying()` callback in the same `onClick`, navigating there automatically - matches the literal instruction "back button returns to Library": `onBack` for `NowPlaying` always goes to `Screen.Home`, not "wherever it was opened from", since that's what was asked for rather than a full back-stack. `BackHandler` (system back button) and an explicit toolbar back arrow both wire to the same `onBack`. Every other screen keeps its own `PlaybackBar` in `bottomBar` unchanged, so the compact bar still shows everywhere except the full-screen view itself.
- **Verified on-device (POCO X6 Pro 5G, signed release build, installed with `adb install -r` over the existing 174-track library to genuinely exercise the migration path rather than a clean install)**:
  - **No data loss**: screenshotted the library immediately after the first launch on the new build - all tracks present, same list as before the update. No crash, no migration exception in logcat (checked for `FATAL`/`AndroidRuntime`/exceptions across the whole session - none).
  - **Art renders for a track with known embedded art**: tapped "Animal" (Zayde Wølf) - one of the tracks independently confirmed via `mutagen` to have embedded art - and the Now Playing screen showed the real album cover ("Modern Alchemy", a red-moon illustration), correct title/artist, live seek bar and time counter, working transport controls.
  - **Placeholder renders correctly for a track with no embedded art**: pulled the same `Atlantis.ogg` ringtone used in the prior test and confirmed via `mutagen` it has zero embedded pictures, imported it through the drop-folder flow, tapped it - Now Playing showed the themed purple `MusicNote` placeholder (not a crash, not a blank frame, not a broken-image icon), correct title (no artist row, since the file has none), correct short duration (0:10).
  - **Tap-to-open and back navigation**: both taps above navigated to Now Playing automatically with no extra step. Pressing back from Now Playing returned to the Library screen with playback continuing uninterrupted (confirmed via the mini `PlaybackBar` still showing the same track mid-playback, not reset).
  - **Cleanup**: removed the `Atlantis.ogg` test track via "Delete file" and re-ran "Regenerate AI playlists" afterward so the real 174-track library's playlists reflect only real data - confirmed the same 5-cluster distribution as before the test (`Calm/Fast`, `Moderate Energy/Fast` x2, `Moderate Energy/Mid-Tempo` x2).

## Audio focus handling + rotating Now Playing art (2026-09-03, done and verified on-device)

- **Audio focus - not previously configured at all**: `PlaybackService`'s `ExoPlayer.Builder(this).build()` used the library default, which does *not* enable focus handling by itself - confirmed by reading the file before assuming Media3's usual defaults applied. Fixed by explicitly building `AudioAttributes` (`USAGE_MEDIA` / `AUDIO_CONTENT_TYPE_MUSIC`) and passing them via `setAudioAttributes(audioAttributes, /* handleAudioFocus= */ true)` on the `ExoPlayer.Builder` - this hands focus request/abandon and the pause-on-loss/duck-on-transient behavior entirely to ExoPlayer's built-in `AudioFocusManager`, no custom `AudioManager`/`AudioFocusRequest` code needed.
- **Resume-after-transient-loss is also ExoPlayer's built-in behavior, not something added here**: a permanent focus loss (another app taking over playback outright, e.g. YouTube) leaves `playWhenReady = false` with no auto-resume; only a *transient* loss (e.g. a short notification sound) auto-resumes when focus returns. This matches the requested behavior ("stay paused until manually resumed" for the YouTube case) without any extra code - verified on-device rather than assumed, per instructions.
- **Rotating album art**: `NowPlayingScreen`'s art `Box` gained an `Animatable(0f)` (`artRotation`) driving `Modifier.rotate(artRotation.value)`. A `LaunchedEffect(state.isPlaying)` runs `artRotation.animateTo(artRotation.value + 360f, infiniteRepeatable(tween(20_000ms, LinearEasing)))` only while playing; when `isPlaying` flips to `false` the effect's key changes, the coroutine is cancelled, and the `Animatable` simply stops at whatever angle it was mid-animation - no explicit pause/resume state needed, and no toggle added (defaulted on, per instructions, since it's a repeat request). Resuming re-enters the same `LaunchedEffect` branch and continues from the current `artRotation.value`, not a reset to 0, so the spin doesn't visibly jump.
- **Verified on-device (POCO X6 Pro 5G, signed release build installed with `adb install -r` over the existing library)**:
  - **Rotation while playing**: started "Lights Down Low", screenshotted twice ~3s apart on the Now Playing screen - the art was at a visibly different angle each time (position also advanced 0:06 -> 0:21), confirming continuous rotation, not a static frame.
  - **Freeze on pause**: tapped pause - play icon appeared, position froze at 0:54, and a follow-up screenshot 3s later showed the identical angle and position, confirming the animation genuinely stops in place rather than just rendering slowly.
  - **Resume continuity**: tapped play again - position advanced (0:54 -> 0:55) and the art continued rotating from the same angle it had frozen at, not from 0.
  - **Audio focus - pause on interruption**: with Nera Music playing, launched a real YouTube video ("Me at the zoo") via `am start -a VIEW`. `dumpsys media_session` confirmed Nera Music's session flipped to `state=PAUSED` while YouTube's session became active - genuine OS-level audio focus preemption, not a guess based on UI alone.
  - **No auto-resume after the other app stops**: force-stopped YouTube, re-checked `dumpsys media_session` - Nera Music's session was still `state=PAUSED` at the same position, confirming it did not auto-resume. Foregrounding the app afterward showed the same paused state in the UI.

## Circular rotating art + a Settings toggle for it (2026-09-03, done and verified on-device)

- **Follow-up to the previous section**: that entry's "no toggle added" note is superseded here - a toggle was requested and added the same day.
- **Circular clip tied to the toggle, not to whether it's currently spinning**: `NowPlayingScreen`'s art `Box` now clips to `CircleShape` when the new "Rotate album art" setting is on, and `RoundedCornerShape(24.dp)` (the original square-ish look) when it's off - `Modifier.clip(if (rotateAlbumArt) CircleShape else RoundedCornerShape(24.dp))`. Chose to key the shape off the setting rather than off `isPlaying`: a paused disc should still look like a disc, not snap back to a square the instant playback pauses, since the circular "record" framing is the point of the effect, not just a side effect of the animation being active at that exact moment. Same reasoning extends the rotation `LaunchedEffect`'s condition to `state.isPlaying && rotateAlbumArt`, and the applied rotation angle to `if (rotateAlbumArt) artRotation.value else 0f`, so turning the setting off both stops the spin and reverts the shape together, with no in-between state where a square is spinning or a circle is static-by-accident.
- **New setting, same pattern as `showAnalysisBadges`**: `LibraryDisplayPreferences` gained a second persisted boolean, `rotateAlbumArt` (key `rotate_album_art`, default `true`), with matching getter/setter in `SettingsViewModel`. `SettingsScreen` gained a second `Switch` row under the existing "Library" section, labeled "Rotate album art". `NowPlayingScreen` now takes a `settingsViewModel: SettingsViewModel` param (`NeraApp` already held one instance shared with `SettingsScreen`, so no new ViewModel instance was created - toggling in Settings and viewing Now Playing read the same state) and collects `rotateAlbumArt` from it.
- **Verified on-device (POCO X6 Pro 5G, signed release build, installed with `adb install -r` over the existing library)**:
  - **Circular clip, no distortion**: opened Now Playing on "Animal" (known embedded art) with the toggle at its default (on) - the art rendered as a clean circle with no stretching or squashing of the image content, `ContentScale.Crop` filling the circular frame correctly from the square source image.
  - **Rotation still animates correctly inside the circular clip**: two screenshots ~3s apart showed the art at visibly different angles (a label on the source image visibly moved position between frames) while staying perfectly circular throughout - confirmed the clip and the rotation modifier compose correctly together, not that adding the clip broke the animation.
  - **Toggle off -> static square**: switched "Rotate album art" off in Settings, reopened Now Playing on the same track - the art immediately showed as the original square-with-rounded-corners (uncropped - text along the image edges that the circular clip had been cutting off was now visible again), and two screenshots ~3s apart (position advancing normally, 0:00 -> 0:18) showed the exact same static angle, confirming no rotation runs while the setting is off.
  - **Toggle restored to on** after verification, so the phone is left in the shipped default state rather than whatever state manual testing happened to leave it in.